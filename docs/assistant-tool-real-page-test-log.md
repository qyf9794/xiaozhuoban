# Assistant Tool Real Page Test Log

Date: 2026-06-17
Target: https://xiaozhuoban.bqxb.org/app
Mode: skip gpt-realtime-2 first; drive the assistant through the on-page text command input and verify real UI state.

## Milestones

### M1 Desktop Board Tools

Goal: prove the stage-one desktop tools work from the real page command path before voice.

Tools:
- `board.add_widget`
- `widget.focus`
- `widget.move`
- `widget.resize`
- `widget.bring_to_front`
- `board.auto_align`
- `assistant.confirm`
- `assistant.cancel`

Acceptance:
- Each command produces a visible success/error message in the assistant dock.
- Board/widget DOM state changes match the command.
- Confirmation flows show and clear the pending state.
- Results are recorded with command text, evidence, and status.

### M2 Board Management Tools

Goal: verify board lifecycle tools on the real page without relying on Realtime.

Tools:
- `board.create`
- `board.rename`
- `board.switch`

Acceptance:
- New board appears in the board list.
- Rename persists in visible UI.
- Switch changes the active board and assistant context.

### M3 Widget Detail State Tools

Goal: verify widget-detail tool calls after focusing or adding a widget.

Tools:
- `note.append`
- `note.set_text`
- `note.clear`
- Clock/countdown/weather/search/video/music/detail actions exposed by `widgetStateActions`.

Acceptance:
- Focusing a widget updates available tool scope.
- Each tested detail command changes the target widget state or returns an expected guarded response.
- Failed or unsupported detail commands are logged with the exact blocker.

### M4 Mounted Capability Tools

Goal: verify tools backed by mounted widget capabilities.

Tools:
- TV/media playback controls
- recorder actions
- music player actions
- dial clock/night mode and other `widgetCapabilityBridge` actions

Acceptance:
- Capability is registered only when the widget is mounted.
- Command reaches the mounted widget.
- UI state or widget-local evidence proves the action ran.

### M5 Concurrency And Status Bubble

Goal: add and verify concurrent tool execution visibility.

Required work:
- Add a state bubble showing current operation name/status.
- Preserve visible state for queued, running, succeeded, failed, and waiting-confirmation operations.
- Verify rapid multi-command behavior does not lose state or hide failures.

Acceptance:
- Real page shows operation state during tool execution.
- Test log records parallel or rapid command behavior.
- Any code changes are tested, committed, and pushed before moving on.

### M6 Voice Handoff

Goal: after command-path tools are reliable, return to `gpt-realtime-2` voice.

Required work:
- Tune turn detection if voice is cut off before the user finishes speaking.
- Verify voice calls the same tools already proven through text commands.

Acceptance:
- Microphone permission is allowed.
- A long spoken command is not prematurely cut off.
- At least one board tool and one widget-detail tool succeed by voice.

## Test Results

### M1 Desktop Board Tools

Status: in progress

| Time | Tool | Command | Evidence | Result | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-06-17 01:03 CST | `board.add_widget` | `添加便签` | Chrome real page showed the command text inside the assistant input, but pressing Enter left `inputValue="添加便签"`, dock text unchanged, and widget count stayed at 6. Clicking the send button failed because the automation click target resolved outside the visible viewport. | fail | First blocker is testability/observability of the dock command path on mobile layout. Add stable test ids and an operation status bubble before continuing real-page tool tests. |
| 2026-06-17 01:06 CST | M1-0 testability | N/A | Added `data-testid` hooks for the dock/input/send/status bubble and a visible operation bubble. Verified with `pnpm --filter @xiaozhuoban/web test -- src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This does not prove tool execution yet; it makes the real-page command path observable and easier to drive in the next M1 run. |
| 2026-06-17 01:08 CST | M1-0 deployment check | N/A | Production asset polling showed CSS changed from `index-CfYqnNKp.css` to `index-CSXI5C-X.css`, matching the local build asset for the operation bubble change. | pass | Deployment reached production assets. Chrome page-level verification was attempted next. |
| 2026-06-17 01:09 CST | Chrome automation | N/A | `browser.user.openTabs()` and `browser.tabs.list()` both saw the Xiaozhuoban tab, but page interaction calls repeatedly failed with `Tab not found` or session/tab ownership mismatch after reload/new-tab attempts. | fail | This confirms the user-reported Chrome tool instability is affecting real-page tests. Continue M1 only after reconnecting a stable Chrome tab, or use another approved browser surface for verification. |
| 2026-06-17 01:10 CST | command submit fallback | `添加便签` | Added `resolveVoiceAssistantSubmitText` so form submit falls back to the real input DOM value when React state is empty, and added `onInput` synchronization for external automation/voice-like input. Verified with `pnpm --filter @xiaozhuoban/web test -- src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This directly addresses the observed failure where the command text was visible in the input but the command path did not run. Real-page retest is still required after deploy. |
| 2026-06-17 01:11 CST | command submit fallback deployment | N/A | Production `index-DUZWtNyq.js` dynamically references `App-BPa7pCub.js`; that App chunk contains `voice-assistant-command-input`, `voice-assistant-send`, and `voice-assistant-operation`. | pass | The command fallback build reached production assets. |
| 2026-06-17 01:11 CST | Chrome automation retry | N/A | After reconnecting Chrome, `browser.tabs.list()` showed only the Xiaozhuoban tab, but `browser.tabs.get('1115457935')` followed by page calls still failed with `Tab not found: 4. Existing tabs: none`. | fail | Chrome tool session remains unstable, so M1 real-page command retest is blocked on Chrome automation rather than app code evidence. |
| 2026-06-17 01:14 CST | command Enter fallback | `添加便签` | Added `shouldSubmitVoiceAssistantOnKeyDown` and an input-level plain-Enter handler that calls the same DOM fallback submit path while ignoring Shift/Ctrl/Meta/Alt/composition Enter. Verified with `pnpm --filter @xiaozhuoban/web test -- src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This addresses the observed real-page symptom where pressing Enter left the command in the input and did not run the tool path. Real-page retest still depends on stable browser automation. |
| 2026-06-17 01:15 CST | command Enter fallback deployment | N/A | Production `/app` entry references `assets/App-BPa7pCub.js`, and that chunk contains the Enter fallback markers `isComposing`/`onKeyDown`. | pass | Enter fallback reached production assets. |
| 2026-06-17 01:17 CST | command send button fallback | `添加便签` | Changed the send button to stay enabled unless the assistant is muted, so DOM-backed command fallback is not blocked when React `text` state lags behind the real input value. Verified with `pnpm --filter @xiaozhuoban/web test -- src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Empty commands remain safe because `runCommand` trims and no-ops empty input. Real-page retest still depends on stable browser automation. |
| 2026-06-17 01:18 CST | command send button fallback deployment | N/A | Production `/app` entry changed to `index-0Jlg6l9J.js`, which references `assets/App-CX-fitcz.js`; the App chunk contains `voice-assistant-send` and the updated send-button code. | pass | Send-button fallback reached production assets. |
| 2026-06-17 01:21 CST | tool operation status events | Realtime/function-call tools | Added `AssistantOperationEvent` emission from `AssistantHarness` for running, waiting-confirmation, success, and failed tool phases; wired runtime/App/Dock so external tool calls can update the visible operation bubble even when they do not originate from the text form. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This is the first state-bubble layer for Realtime tool calls. It does not yet prove parallel execution; it makes each tool phase observable in UI. |
| 2026-06-17 01:23 CST | tool operation status deployment | N/A | Production `/app` entry `assets/index-BLGw3LWO.js` references `assets/App-BiJBUfuz.js`; the App chunk contains the operation-state markers used by the new Realtime/function-call status bridge. | pass | Tool operation status UI reached production assets. |
| 2026-06-17 01:27 CST | concurrent operation status aggregation | Realtime/function-call tools | Added an operation snapshot reducer that tracks multiple active tool calls by id, removes completed calls, and summarizes concurrent running/waiting-confirmation operations for the visible Dock bubble. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantOperationStatus.test.ts src/assistant/AssistantHarness.test.ts src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This prepares the UI/state layer for concurrent tool events. Realtime session config still has model-side `parallel_tool_calls: false`, so the next step is a controlled direct-command/concurrency test before enabling model-side parallel calls. |
| 2026-06-17 01:28 CST | concurrent operation status deployment | N/A | Production `/app` entry `assets/index-BlK7NEmB.js` references `assets/App-DtaoaRee.js`; the App chunk contains the concurrent operation summary markers. | pass | Concurrent operation status aggregation reached production assets. |
| 2026-06-17 01:30 CST | real-page direct command path | `添加便签`, `添加待办` | In the in-app browser on production `/app`, `添加便签` cleared the input and completed as `已聚焦小工具` because a note widget already existed. `添加待办` then cleared the input, changed todo widget headings from 0 to 1, and showed `完成：已添加小工具`. | pass | This proves the text command path and `board.add_widget` shortcut path work on the real production page without using Realtime. Chrome automation remains separately unstable. |
| 2026-06-17 01:32 CST | real-page confirmation flow | `整理桌面`, `取消` | In the in-app browser on production `/app`, `整理桌面` cleared the input, showed `待确认：整理桌面`, displayed `确认`/`取消` buttons, and set the last message to `确认执行 board.auto_align 吗？`. Clicking `取消` removed the buttons and showed `完成：已取消`. | pass | This verifies the confirm/cancel tool flow on the real page without mutating the desktop layout. |
| 2026-06-17 01:34 CST | real-page widget detail tool | `北京天气`, `上海天气` | Initial production test returned `未知工具：weather.set_city`, revealing widget detail actions were filtered out when the Harness initialized before widget definitions were loaded. Fixed detail-action registration, verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. After production loaded `assets/index-B1wkgMbt.js`, `北京天气` switched the weather widget to 北京 and `上海天气` restored 上海, both showing `完成：已切换天气城市`. | pass | This verifies a widget-detail shortcut tool on the real page and records the cache-bust needed to load the fixed production entry. |
| 2026-06-17 01:36 CST | Realtime session endpoint precheck | N/A | `POST https://xiaozhuoban.bqxb.org/api/realtime/session` returned HTTP 200 with `model: gpt-realtime-2` and a present client secret field. The secret value was not logged. | pass | Backend API key/session configuration is currently present; no user-side API key action is needed before the next Realtime connection test. |
| 2026-06-17 01:40 CST | Realtime connection browser precheck | `连接语音` | In the in-app browser, clicking `连接语音` showed `失败：麦克风权限被拒绝，请在浏览器地址栏允许麦克风后重试。`. The same test on the claimed Chrome `https://xiaozhuoban.bqxb.org/app` tab also returned the same microphone-denied UI state, with no console errors captured. | blocked | This is not an API key or session endpoint problem; the browser is denying microphone access before WebRTC reaches the Realtime SDP stage. Re-enable microphone permission for `xiaozhuoban.bqxb.org`, then rerun the Realtime connection test. |
| 2026-06-17 01:44 CST | Realtime microphone diagnostics | `连接语音` | Added a Realtime adapter microphone permission precheck, missing-device/unavailable classification, and separate UI copy for `MICROPHONE_UNAVAILABLE` versus `MICROPHONE_DENIED`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/components/VoiceAssistantDock.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This does not bypass the current browser permission block; it makes the next Realtime connection test more diagnostic once microphone access is allowed. |
| 2026-06-17 01:45 CST | Realtime microphone diagnostics deployment | N/A | Production `/app` entry `assets/index-hmf6KKlz.js` references `assets/App-aNapjc-U.js`; the App chunk contains the new microphone diagnostics markers. | pass | The permission/unavailable diagnostic layer reached production assets. |
| 2026-06-17 01:47 CST | Realtime turn detection tuning | N/A | Changed the Realtime session payload to use `semantic_vad` with `eagerness: low`, `create_response: true`, and `interrupt_response: true`, and added a helper so eagerness can be tuned later. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/realtimeSessionConfig.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This prepares the next voice-cutoff test by making turn detection wait longer before treating the user's speech as finished. It still requires browser microphone permission before real audio can be verified. |
| 2026-06-17 01:53 CST | Realtime turn detection API sync | N/A | Synced the same conservative `semantic_vad` payload into the production API entry `apps/web/api/realtime/session.ts` and extended `api/realtime/session.test.ts` to assert `eagerness: low`, `create_response: true`, and `interrupt_response: true` in the body sent to OpenAI. Verified with `pnpm --filter @xiaozhuoban/web test -- api/realtime/session.test.ts src/assistant/realtimeSessionConfig.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This corrects the earlier frontend-only VAD tuning so the actual serverless session endpoint will send the conservative turn-detection config after deployment. |
| 2026-06-17 01:56 CST | Realtime session config observability | N/A | Added non-sensitive response header `x-xiaozhuoban-realtime-turn-detection: semantic_vad;eagerness=low` to successful `/api/realtime/session` responses and covered it in `api/realtime/session.test.ts`. Verified with `pnpm --filter @xiaozhuoban/web test -- api/realtime/session.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This gives the next production check a direct way to verify that the serverless API is running the conservative VAD config without logging secrets. |
| 2026-06-17 01:58 CST | Realtime session config deployment | N/A | Production `POST https://xiaozhuoban.bqxb.org/api/realtime/session` returned HTTP 200 with header `x-xiaozhuoban-realtime-turn-detection: semantic_vad;eagerness=low` on the third poll. | pass | The serverless Realtime session endpoint is now confirmed to be serving the conservative VAD config. |
| 2026-06-17 02:00 CST | Realtime parallel tool calls | N/A | Enabled `parallel_tool_calls: true` in both shared Realtime config and production `/api/realtime/session`, added non-sensitive response header `x-xiaozhuoban-realtime-parallel-tools: true`, and updated API/shared tests to assert the payload and header. Verified with `pnpm --filter @xiaozhuoban/web test -- api/realtime/session.test.ts src/assistant/realtimeSessionConfig.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This enables model-side parallel tool calls for `gpt-realtime-2`; UI state aggregation was already added earlier. Real audio/tool verification still requires microphone permission. |
| 2026-06-17 02:01 CST | Realtime parallel tool deployment | N/A | Production `POST https://xiaozhuoban.bqxb.org/api/realtime/session` returned HTTP 200 with header `x-xiaozhuoban-realtime-parallel-tools: true` on the fourth poll and retained `x-xiaozhuoban-realtime-turn-detection: semantic_vad;eagerness=low`. | pass | The serverless Realtime session endpoint is confirmed to be serving model-side parallel tool calls. |
| 2026-06-17 02:03 CST | Realtime function-call dispatch | Back-to-back data-channel events | Extracted the Realtime function-call dispatch path into a tested helper and verified that two distinct call ids dispatched back-to-back are both handled, duplicate call ids are ignored, and malformed events do not block the next valid call. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This strengthens the non-microphone M5 coverage for rapid/parallel tool-call dispatch. Real audio/tool verification still requires microphone permission. |
| 2026-06-17 02:04 CST | Realtime function-call dispatch deployment | N/A | Production `/app` entry `assets/index-CPyz3fIw.js` references `assets/App-nNFjWhVY.js`; the App chunk contains the Realtime function-call event markers `response.function_call_arguments.done`, `response.output_item.done`, and the `handledFunctionCallIds` path. | pass | The tested function-call dispatch path reached production assets. |
| 2026-06-17 02:07 CST | M2 board create/rename shortcut routing | `新建桌板叫测试桌板`, `把当前桌板重命名为工作台` | Added shortcut-first routing for `board.create` and active-board `board.rename`, passed active board id/name into the Harness shortcut context, and added acceptance coverage proving both commands run without model fallback. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This completes the first M2 slice for board lifecycle tools. `board.switch` still needs board-list context before it can be routed safely by text command. |
| 2026-06-17 02:08 CST | M2 board create/rename real-page verification | `新建桌板叫测试桌板`, `把当前桌板重命名为工作台测试` | Production `/app` loaded `assets/index-B5Ekl_aZ.js` -> `assets/App-BhpOd2PK.js` with the new route markers. In the in-app browser, the first command created and switched to `测试桌板` with operation `完成：已新建桌板`; the second command renamed it to `工作台测试` with operation `完成：已重命名桌板`, and the board list showed `默认桌板` plus `工作台测试`. | pass | This proves `board.create` and active-board `board.rename` work from the real page text command path without Realtime. |
| 2026-06-17 02:12 CST | M2 board switch shortcut routing | `切换到我的桌板`, `切换到工作台测试桌板` | Added board-list context to assistant summaries and shortcut context, routed board-name switch commands to `board.switch`, and verified shortcut-first Harness execution without model fallback. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This completes M2 board lifecycle routing in automated coverage. Real-page switch verification follows after deployment. |
| 2026-06-17 02:14 CST | M2 board switch real-page verification | `切换到工作台测试桌板`, `切换到默认桌板` | Production `/app` loaded `assets/index-D-2Uu4QM.js` -> `assets/App-Cemas_08.js` with the new board-list/switch route markers. In the in-app browser, `切换到工作台测试桌板` changed the active board to `工作台测试` and showed `完成：已切换桌板`; `切换到默认桌板` changed the active board back to `默认桌板` and again showed `完成：已切换桌板`. | pass | This completes M2 board lifecycle real-page verification for create, rename, and switch without Realtime. |
| 2026-06-17 03:28 CST | Tool stage gate removal | `大富翁掷骰`, `提交这个 AI 表单`, `帮我生成一个新工具`, `帮我重写这篇长文` | Removed the local `assistant.out_of_scope` shortcut guardrail and stopped registering the guardrail action, removed `assistant.out_of_scope` from initial Realtime tools, and rewrote Realtime instructions so registered tools are not described as stage-limited. Verified source search no longer finds runtime `assistant.out_of_scope`/`OUT_OF_SCOPE`/`这一阶段`/`没开放` messages, then ran `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/realtimeSessionConfig.test.ts api/realtime/session.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This opens the request path so unsupported commands now fall through to model/registered-tool handling instead of the local stage gate. The next work remains to add or repair concrete tool implementations one by one. |
| 2026-06-17 03:30 CST | Tool stage gate real-page verification | `帮我生成一个新工具` | Production `/app` loaded `assets/index-JnMiXBND.js` -> `assets/App-f5KXWKpf.js`, where old `assistant.out_of_scope`/`OUT_OF_SCOPE`/`这一阶段`/`没开放` markers were absent and new registered-tool wording was present. In the in-app browser, sending `帮我生成一个新工具` cleared the input and showed `完成：我没听懂，可以再说短一点吗？`; the page contained no `这一阶段`/`本阶段`/`没开放`/`没有开放`/`下一阶段` text. | pass | This proves the production text-command path no longer blocks requests with the previous stage-gate message. |
| 2026-06-17 03:35 CST | M3 daily widget shortcut routing | `便签记下明早九点开会`, `添加待办买牛奶`, `保存到剪贴板账号是 demo` | Added shortcut-first routes for note writing, todo item creation, and clipboard text saving. Existing widgets receive `note.write` / `todo.add_item` / `clipboard.add_text`; a missing clipboard widget can be added with a `board.add_widget` follow-up. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/widgetStateActions.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This covers the first daily-widget reliability slice without testing games or AI forms. Real-page verification follows after deployment. |
| 2026-06-17 03:38 CST | M3 close widget shortcut routing | `关闭音乐`, `暂停音乐` | Added a close-widget shortcut so `关闭` / `关掉` plus a widget name routes to `widget.remove`, while playback wording such as `暂停` / `停一下` / `停止` continues to route to the media pause action. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | `关闭音乐` now means closing the music widget window and should require the normal destructive-action confirmation before deletion. |
| 2026-06-17 03:42 CST | M3 open/close music real-page verification | `打开音乐`, `关闭音乐`, `确认`, `打开音乐` | Production `/app` loaded `assets/index-EdssggPQ.js` -> `assets/App-DHAFiu5l.js`, containing `not_close_widget`, `close_widget_target_missing`, `widget.remove`, and `music.pause` markers. In the in-app browser, `打开音乐` returned `完成：已聚焦小工具`; `关闭音乐` returned `待确认：关闭音乐` with visible `确认`/`取消`; clicking `确认` returned `完成：已删除小工具` and removed the `音乐播放器` heading; a final `打开音乐` returned `完成：已添加小工具` and restored the music widget. | pass | This verifies the intended daily meaning: open shows/adds a widget window, close removes the widget window after confirmation, and pause remains the playback-control wording. |
| 2026-06-17 03:52 CST | M4 broader daily command capture and scoped Realtime text fallback | `关音乐`, `启动音乐播放器`, unmatched daily wording | Expanded local open/close widget aliases, then added `/api/realtime/tool-call` as a two-pass text fallback. Pass 1 sends only the user command and tool catalog to select a tool; pass 2 sends only the selected tool plus scoped context for that tool, so unrelated widget ids and private summaries are not sent to `gpt-realtime-2`. The frontend text fallback now calls this endpoint instead of returning the local empty fallback. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- api/realtime/tool-call.test.ts src/assistant/realtimeTextToolCall.test.ts src/assistant/openaiRealtimeAdapter.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Backend `OPENAI_API_KEY` is still required. Optional `XIAOZHUOBAN_TEXT_TOOL_MODEL` can override the default `gpt-realtime-2` text fallback model if production model support requires it. |
| 2026-06-17 04:02 CST | M4 scoped text fallback production endpoint check | `麻烦把音乐窗口收起来` | Production `/api/realtime/tool-call` now returns structured JSON instead of a function-start failure. The endpoint reported `TEXT_TOOL_SELECTION_FAILED` with upstream OpenAI `model_not_found` for `gpt-realtime-2` on the Responses text endpoint. | blocked | Configure backend env `XIAOZHUOBAN_TEXT_TOOL_MODEL` to a Responses-compatible text model, then rerun this endpoint and real-page fallback test. `gpt-realtime-2` remains available for the WebRTC Realtime session endpoint. |
| 2026-06-17 04:10 CST | M5 daily utility shortcut routing | `把你好翻译成英文`, `12米换算成公里`, `计算 12+30`, `看标普和恒生行情`, `世界时钟显示北京伦敦纽约`, `刷新新闻` | Added shortcut-first routing for translate drafts, converter setup, calculator display results, market index selection, world clock zones, and headline refresh. Also changed local shortcut target resolution to use the full widget list instead of the compact Realtime summary, so later widgets beyond the first eight can still be controlled reliably. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/widgetStateActions.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This milestone avoids game and AI form testing and expands reliable daily utility commands that do not require the model fallback. |
| 2026-06-17 04:23 CST | Realtime context scoping | Voice/WebRTC tool routing | Changed the Realtime session architecture so the initial `gpt-realtime-2` session exposes only `assistant.select_tool`; `updateContext` now caches desktop context locally instead of sending it over the data channel. After realtime selects a tool, the frontend sends a scoped session update containing only the selected tool and minimal target context. The `/api/realtime/tool-call` fallback also accepts a preselected tool and skips first-pass selection. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/realtimeTextToolCall.test.ts src/assistant/openaiRealtimeAdapter.test.ts api/realtime/tool-call.test.ts src/assistant/realtimeSessionConfig.test.ts api/realtime/session.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This implements the required two-stage flow: realtime first chooses the tool, then only that tool's scoped context is provided before the model returns executable operation info to the frontend. |
| 2026-06-17 04:25 CST | Realtime selector-only production check | `POST /api/realtime/session` | Production returned HTTP 200 with `x-xiaozhuoban-realtime-tool-stage: selector-only` on the second poll, while retaining `x-xiaozhuoban-realtime-turn-detection: semantic_vad;eagerness=low` and `x-xiaozhuoban-realtime-parallel-tools: true`. No client secret or response body was logged. | pass | Confirms Vercel is serving the selector-only Realtime session stage. |
| 2026-06-17 04:36 CST | M6 MusicKit music widget foundation | `播放周杰伦音乐`, `下一首音乐`, music widget UI | Added a MusicKit client helper, Developer Token detection via `VITE_APPLE_MUSIC_DEVELOPER_TOKEN`, lazy MusicKit JS loading/configuration, Apple Music authorization entry, normalized song/album/playlist search results, queue descriptors, top artwork area, playback controls, search field, and queue list. When the token is absent, the widget keeps the existing iTunes 30-second preview fallback. Expanded shortcut routing so music play commands carry a query and next-track commands call `music.next`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/widgets/musicKitClient.test.ts src/assistant/widgetCapabilityBridge.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Full production Apple Music login/playback still requires Vercel env `VITE_APPLE_MUSIC_DEVELOPER_TOKEN` and a redeploy before real account verification. |
| 2026-06-17 04:40 CST | M6 music capability registration fix | `播放周杰伦音乐`, `下一首音乐` | Production verification found the new music shortcut routing reached `music.play` / `music.next`, but the Harness returned `未知工具` because mounted capability actions were filtered during early initialization before widget definitions loaded. Changed capability actions to register stably and validate targets at execution time. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts src/widgets/musicKitClient.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production retest follows after deployment of this fix. |
| 2026-06-17 04:43 CST | M6 iTunes fallback playback handling | `播放周杰伦音乐` without Apple Music token | In preview fallback mode, browser autoplay can reject delayed audio playback after search. Changed the fallback result to treat the search/queue action as successful and tell the user to click play, while keeping Apple Music playback failures explicit. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts src/widgets/musicKitClient.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This makes no-token production behavior less alarming while preserving the need for `VITE_APPLE_MUSIC_DEVELOPER_TOKEN` for full Apple Music playback. |
| 2026-06-17 04:45 CST | M6 music production command retest | `播放周杰伦音乐` | Production `/app` loaded the final music build with `iTunes 试听模式`. In the in-app browser, the command returned `完成：已找到音乐，请手动点击播放`, did not show `未知工具`, did not show hard `音乐播放失败`, and the page contained Jay/周杰伦 search results. | pass | Confirms the no-token fallback path and music capability registration fix are live. Full Apple Music account playback remains pending `VITE_APPLE_MUSIC_DEVELOPER_TOKEN`. |
| 2026-06-17 05:00 CST | Realtime text fallback two-phase protocol | `phase=select`, `phase=execute` | Split the frontend text fallback into two backend calls: the first request sends only `input`, `tools`, and `phase: select`; after tool selection, the frontend locally scopes context and sends `phase: execute` with only the selected tool's target context. The backend now accepts select requests without `context`, requires selection/context for execute, and keeps the old auto path for compatibility. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/realtimeTextToolCall.test.ts src/assistant/openaiRealtimeAdapter.test.ts api/realtime/tool-call.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | This tightens the earlier selector-only architecture so the client no longer sends full desktop context even to the text fallback endpoint before Realtime chooses a tool. |
| 2026-06-17 05:04 CST | Realtime scoped tool parameter schema | `widget.remove` after `phase=execute` | Production `phase=execute` initially returned `widget.remove` with empty arguments, revealing that scoped Realtime/text tool schemas did not require `widgetId`. Added parameter inference for desktop target tools and `requiresTarget` widget-detail tools in both shared Realtime serialization and the serverless text fallback serializer. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/realtimeTextToolCall.test.ts src/assistant/openaiRealtimeAdapter.test.ts api/realtime/tool-call.test.ts src/assistant/realtimeSessionConfig.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | The next production retest should require and return target arguments such as `widgetId` instead of only returning the tool name. |
| 2026-06-17 05:07 CST | Realtime two-phase production retest | `关音乐` via `phase=select` then `phase=execute` | Production `/api/realtime/tool-call` returned `selection: { name: "widget.remove", targetHint: "音乐" }` for a select request containing only `input`, `tools`, and `phase`. A following execute request with only the scoped music widget context returned `call.name: "widget.remove"` and `arguments.widgetId: "wi_music"` on the second deployment poll. | pass | Confirms the deployed text fallback now follows the required two-step flow and returns frontend-executable target arguments without sending full desktop context up front. |
| 2026-06-17 05:10 CST | M7 recorder and dial clock shortcut routing | `开始录音`, `停止录音`, `播放录音`, `暂停录音`, `进入时钟夜间模式`, `关闭时钟夜间模式` | Added shortcut-first routing for recorder start/stop/play/pause and dial clock night-mode on/off. `开始录音` can add the recorder widget before running `recorder.start`; playback-only recorder commands require an existing recorder and no longer fall through to focused TV playback. Night-mode commands route to `dialClock.set_night_mode` before close-widget matching, so `关闭时钟夜间模式` changes the clock state instead of removing the widget. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production verification should use safe text commands for night mode and avoid starting microphone recording unless the browser permission prompt is intentionally accepted. |
| 2026-06-17 05:15 CST | M7 recorder and dial clock production verification | `进入时钟夜间模式`, `关闭时钟夜间模式`, `打开录音`, `播放录音`, `暂停录音`, `停止录音`, `关闭录音`, `确认` | Production `/app` loaded the M7 route markers `recorder_action_missing` and `dial_clock_night_mode`. In the in-app browser, `进入时钟夜间模式` returned `完成：已进入夜间模式`, added `is-night-mode`, and changed the clock button to `退出夜间模式`; `关闭时钟夜间模式` returned `完成：已退出夜间模式` and restored `进入夜间模式`. `打开录音` added the recorder widget; `播放录音` returned `完成：没有可播放的录音`; `暂停录音` returned `完成：已暂停录音`; `停止录音` returned `完成：当前没有正在录音`. `关闭录音` showed `待确认：关闭录音`; clicking `确认` returned `完成：已删除小工具` and removed the recorder widget. | pass | This verifies the real text-command path without microphone access: recorder playback commands do not fall through to music/TV, and clock night-mode commands no longer trigger widget deletion. |
| 2026-06-17 05:30 CST | M8 MusicKit production search reliability | `周杰伦` in music widget search | Hardened MusicKit initialization so a `configure()`-returned instance is accepted when `getInstance()` is not ready, changed authorization status reads to be defensive, added MusicKit v3 passthrough search support, added direct Apple Music Catalog API fallback with storefront inference, and normalized wrapped `results` payloads. Verified with `pnpm --filter @xiaozhuoban/web test -- src/widgets/musicKitClient.test.ts src/assistant/widgetCapabilityBridge.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Production `/app` loaded `assets/index-olp8GoD_.js` -> `assets/App-Blq7ietH.js`; searching `周杰伦` returned Apple Music results including `Qi-Li-Xiang`, `Sunny Day`, albums, and multiple `Jay Chou` entries. | pass | The previous production errors `Cannot read properties of undefined (reading 'isAuthorized')` and `F.search is not a function` no longer appear. Apple account playback still requires the user to explicitly click/login through Apple's `允许` authorization flow; this test did not approve that account permission. |
| 2026-06-17 05:50 CST | M9 message board assistant send production verification | `留言板发一句 Codex M9 留言板验证 0551` | Production `/app` served `assets/index-BvLFwmim.js` -> `assets/App-ByBMHXoE.js`, and the App chunk contained `messageBoard.send`, `MESSAGE_AUTH_REQUIRED`, and `已发送留言`. A previously opened Chrome tab initially ran an older in-memory bundle and returned `完成：我没听懂，可以再说短一点吗？`; after reloading the same production tab, the page restored the logged-in `qyf` session and message board. Sending the command cleared the assistant input, showed operation `完成：已发送留言`, and the message board displayed `qyf：Codex M9 留言板验证 0551`. Chrome console error count was 0. | pass | In-app browser input automation was blocked earlier by a missing virtual clipboard, so this real-page verification used the Chrome extension on the user's logged-in tab. The app path itself succeeds after loading the deployed M9 bundle. |
| 2026-06-17 05:53 CST | M10 clipboard clear shortcut routing | `清空剪贴板历史`, `把剪贴板全部清空，包含固定项` | Added shortcut-first routing for clipboard clearing so clear/delete-history wording calls `clipboard.clear` instead of falling through to generic `widget.remove`. Default clear preserves pinned records; explicit all/fixed wording sets `includePinned: true`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production verification follows after deployment; expected UI path is a destructive confirmation for `clipboard.clear`, not closing the clipboard widget. |
| 2026-06-17 05:54 CST | M10 clipboard clear deployment check | N/A | Production `/app` loaded `assets/index-CfqMpwuL.js` -> `assets/App-COFkEPJT.js`; the App chunk contains the `clipboard_clear` shortcut marker and `clipboard.clear` tool name. | pass | The routing fix is deployed. A real-page command test should verify the confirmation UI and ensure the clipboard widget remains open after `清空剪贴板历史`. |
| 2026-06-17 05:56 CST | M10 clipboard clear real-page verification | `切换到工作台测试桌板`, `打开剪贴板`, `保存到剪贴板 M10 剪贴板临时记录 0555`, `清空剪贴板历史`, `确认`, `切换到默认桌板` | In the Chrome production tab, reloaded `/app`, switched to the existing `工作台测试` board, added the clipboard widget, and added a temporary record. `清空剪贴板历史` cleared the input, showed `待确认：清空剪贴板历史`, displayed `确认`/`取消`, and showed the prompt `确认执行 clipboard.clear 吗？` with no `widget.remove` prompt. Clicking `确认` showed `完成：已清理剪贴板历史`; a scoped check of the clipboard card showed `剪贴板历史记录最近复制文本...点击红色铅笔记录复制内容`, did not include `M10 剪贴板临时记录 0555`, and the widget remained mounted. The page was switched back to `默认桌板`; Chrome console error count was 0. | pass | This verifies the daily meaning split: clear clipboard history is a destructive data action with confirmation, while closing/removing the clipboard widget remains a separate `关闭剪贴板` command. |
| 2026-06-17 06:00 CST | M11 todo complete shortcut routing | `完成待办买牛奶`, `删除待办买牛奶` | Added `todo.complete_item` as a widget-detail action that removes the first todo item matching the requested text and returns `TODO_ITEM_NOT_FOUND` without mutation when nothing matches. Added shortcut-first routing for completion/deletion wording with a named todo item, so `删除待办买牛奶` completes the item instead of falling through to generic widget removal; plain `关闭待办` remains the window-close wording. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production verification follows after deployment; test on a temporary todo item to avoid changing user tasks. |
| 2026-06-17 06:04 CST | M11 todo complete real-page verification | `添加待办 M11 完成测试 0602`, `完成待办 M11 完成测试 0602`, `添加待办 M11 删除测试 0603`, `删除待办 M11 删除测试 0603` | Production `/app` deployed `assets/index-TiUbatN4.js` -> `assets/App-ifSRsR6I.js`, containing `todo.complete_item` and `TODO_ITEM_NOT_FOUND`. Chrome text automation `fill()` hit the known virtual clipboard issue, so the same production tab was driven through DOM keyboard input. Adding each temporary todo returned `完成：已新增待办`; `完成待办 M11 完成测试 0602` and `删除待办 M11 删除测试 0603` each returned `完成：已完成待办`. Scoped todo-card checks showed the temporary items were gone while the todo widget remained mounted (`待办支持子任务的待办清单+`). No `未知工具`, no `已删除小工具`, and Chrome console error count was 0. | pass | This verifies the daily meaning split: named todo completion/deletion removes a todo item, while `关闭待办` remains the command for closing the widget window. |
| 2026-06-17 06:06 CST | M12 note clear shortcut routing | `清空便签内容` | Added `note.clear` as a destructive widget-detail action that clears note content without removing the note widget. Added shortcut-first routing for clear/remove-content wording so `清空便签内容` calls `note.clear`, while `关闭便签` remains the window-close wording. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production verification follows after deployment; test with temporary note content to avoid clearing user notes. |
| 2026-06-17 06:17 CST | M12 note clear real-page verification | `打开便签`, `便签记下 M12 临时便签 0618`, `清空便签内容`, `确认` | Production `/app` loaded `assets/index-BYQ9dzJm.js` and the deployed app chunk containing the M12 `note.clear` path. In the Chrome production tab, reloaded `/app`, switched from `默认桌板` to `工作台测试`, and confirmed the test board initially had no note textarea. `打开便签` returned `完成：已添加小工具`; `便签记下 M12 临时便签 0618` returned `完成：已写入便签` and the note textarea contained the temporary text. `清空便签内容` showed `待确认：清空便签内容`, visible `确认`/`取消`, and the prompt `确认执行 note.clear 吗？` with no `widget.remove` prompt. Clicking `确认` returned `完成：已清空便签`; a scoped note textarea check showed one note widget still mounted with empty content. The page was switched back to `默认桌板`; no `未知工具`, no `已删除小工具`, and Chrome console error count was 0. | pass | This verifies the daily meaning split: clearing note content is a destructive data action with confirmation, while `关闭便签` remains the command for closing the note widget window. |
| 2026-06-17 06:21 CST | M13 countdown control shortcut routing | `停止倒计时`, `继续倒计时`, `重置倒计时`, `关闭倒计时` | Added `countdown.pause`, `countdown.resume`, and `countdown.reset` as safe widget-detail actions that update timer state without removing the countdown widget. Added shortcut-first routing so pause/stop, continue/resume, and reset wording call countdown control tools, while `关闭倒计时` remains `widget.remove`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | pass | Real production verification follows after deployment; test on a short countdown in `工作台测试` and confirm the widget stays mounted after stop/reset commands. |
| 2026-06-17 06:27 CST | M13 countdown control real-page verification | `一分钟倒计时`, `停止倒计时`, `继续倒计时`, `重置倒计时` | Production `/app` loaded `assets/App-CRwXD6wC.js`, which contains `countdown.pause`, `countdown.resume`, and `countdown.reset`. Chrome extension text injection was blocked by the known missing virtual clipboard path, so this real-page verification used the in-app browser on the same production URL. After reloading `/app`, switched to `工作台测试` and confirmed no countdown widget was initially present. `一分钟倒计时` returned `完成：已添加小工具，已设置并启动倒计时` and created a countdown card showing `00:00:57`; `停止倒计时` returned `完成：已暂停倒计时` and the countdown card stayed mounted at `00:00:52`; `继续倒计时` returned `完成：已继续倒计时`; `重置倒计时` returned `完成：已重置倒计时` and the countdown card showed `00:01:00`. The page was switched back to `默认桌板`; no `未知工具`, no `已删除小工具`, and console error count was 0. | pass | This verifies the daily meaning split: stop/pause, continue, and reset control the timer state, while `关闭倒计时` remains the separate window-close command. |
| 2026-06-17 06:31 CST | M14 daily utilities production verification | `把你好翻译成英文`, `12米换算成公里`, `计算 12+30`, `看标普和恒生行情`, `世界时钟显示北京伦敦纽约`, `上海天气`, `刷新新闻` | In the in-app browser on production `/app`, reloaded the page, switched to `工作台测试`, and ran the existing daily utility shortcut routes that previously only had automated coverage. The translation command returned `完成：已添加小工具，已填入翻译内容` and showed `快速翻译` with `你好` and target `英文`; the converter command returned `完成：已添加小工具，已更新换算器` and showed `0.012 km`; the calculator command returned `完成：已添加小工具，已更新计算器` and showed display `42`; the market command returned `完成：已添加小工具，已更新指数选择` and showed `标普500` plus `恒生指数`; the world-clock command returned `完成：已添加小工具，已更新世界时钟` and showed `北京`, `伦敦`, and `纽约`; the weather command returned `完成：已添加小工具，已切换天气城市` and showed Shanghai weather; the headline command returned `完成：已添加小工具，已请求刷新新闻` and displayed a populated `重大新闻` widget. The page was switched back to `默认桌板`; no `未知工具`, no `我没听懂`, no `已删除小工具`, and console error count was 0. | pass | Confirms the broader daily utility text-command paths work on the real production page without needing Realtime or backend model fallback. |
| 2026-06-17 09:00 CST | M15 todo production smoke verification | `打开待办`, `待办添加 M15 测试任务 0903`, `完成待办 M15 测试任务 0903` | In the in-app browser on production `/app`, switched from `默认桌板` to `工作台测试` and started with no todo widget in the test board. `打开待办` returned `完成：已添加小工具` and mounted the `待办` card. `待办添加 M15 测试任务 0903` returned `完成：已新增待办` and the todo widget displayed the temporary task. `完成待办 M15 测试任务 0903` returned `完成：已完成待办`; a scoped todo-widget read showed only `待办`, `支持子任务的待办清单`, and `+`, proving the temporary task was removed while the widget stayed mounted. No `未知工具`, no `我没听懂`, and in-app browser console error count was 0. | pass | This is a focused real-page smoke check for the everyday todo path after the broader utility pass, using text commands and explicitly skipping Realtime-2. |
| 2026-06-17 09:15 CST | M16 todo natural due-time commands | `今天下午三点提醒我 M16 时间测试 0916` | Added shortcut parsing for natural todo due times such as `明天9点` and `今天下午三点`, plus reminder wording like `提醒我`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_2r6oxymhBsvXd4P71evFd51fR45V` for commit `09aee44` built `App-D-mlba79.js`, and the custom domain served that chunk with the new parser markers. In the in-app browser on production `/app`, reloaded the app, switched to `工作台测试`, and sent `今天下午三点提醒我 M16 时间测试 0916`; the assistant returned `完成：已新增待办`, and a scoped todo-card read showed `M16 时间测试 0916` with `截止 2026/6/17 15:00:00`. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | Confirms text/voice-style daily commands can now create visible todo deadlines without needing Realtime-2. The temporary M16 item is intentionally left visible for user inspection. |
| 2026-06-17 09:22 CST | M17 todo due-time text preservation | `待办添加 明天9点前端会议 M17 0920` | Fixed due-time cleanup so deadline suffixes like `前` are stripped only when attached to the time expression, instead of globally deleting the single character from task text. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_7fva7QC5JDuuRfU5a75gZMqWXxg8` for commit `6cfe19e` reached READY and built `App-BXcXNT9D.js`. In the in-app browser on production `/app`, reloaded the app, switched to `工作台测试`, and sent the command; the assistant returned `完成：已新增待办`, and a scoped todo-card read showed `前端会议 M17 0920` with `截止 2026/6/18 09:00:00`. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | Confirms natural time parsing preserves task nouns such as `前端会议` while still supporting deadline wording like `明天9点前交报告`. The temporary M17 item is intentionally left visible for user inspection. |
| 2026-06-17 09:34 CST | M18 expanded time command batch | `1小时30分钟倒计时`, `90秒倒计时`, `待办添加 下周一9点团队会 M18 0931`, `三天后下午4点提醒我寄快递 M18 0932` | Added compound countdown parsing for hours/minutes/seconds and expanded todo due-date parsing for weekday and days-later wording. Pulled Vercel production env vars into ignored local file `apps/web/.env.local`; local `/app` reached the real Supabase-backed login page, but localhost had no authenticated session, so final real-page verification used the logged-in production domain. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_HN2LZKRX4YLqXmSbZY7FEfj7Wdx5` for commit `c5821ce` reached READY and built `App-DXbkB4gU.js`. In the in-app browser on production `/app`, switched to `工作台测试`; `1小时30分钟倒计时` showed `01:29:59`, `90秒倒计时` showed `00:01:27`, `下周一9点团队会` showed `截止 2026/6/22 09:00:00`, and `三天后下午4点提醒我寄快递` showed `截止 2026/6/20 16:00:00`. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | This batch was intentionally grouped into one code commit/push per the new cadence. The log row is recorded locally and should be included with the next grouped commit. |
| 2026-06-17 09:42 CST | M19 cancel countdown wording | `半小时倒计时`, `取消倒计时`, `结束计时器` | Added shortcut coverage so `取消` / `结束` countdown wording pauses the timer instead of falling through to `我没听懂` or closing the widget. Also added a half-hour duration regression. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_6tpyK1wiTCv3cRyYdpb3NnQwohpu` for commit `0def1bc` reached READY and built `App-C6oHymSz.js`. In the logged-in Chrome production tab on `/app`, switched to `工作台测试`; `半小时倒计时` returned success and showed `00:29:59`; `取消倒计时` returned `已暂停倒计时`, kept the countdown widget mounted, and did not show `已删除小工具`. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | In-app browser text entry was blocked by the known missing virtual clipboard path, so production verification used Chrome extension control. The M19 log row is local and should be included with the next grouped commit. |
| 2026-06-17 09:55 CST | M20 broader world-clock city commands | `看东京巴黎悉尼时间`, `汉城和迪拜现在几点` | Expanded shortcut city capture for world-clock commands to include frontend-supported cities such as 巴黎、柏林、东京、首尔、新加坡、迪拜、悉尼 and aliases such as 汉城→首尔, while preserving spoken order and deduping duplicate zones. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_51gvcTRkDkNPb9EQJSxA3hYuLKgZ` for commit `ecf50fe` reached READY and the custom domain served `index-DJbbkG-y.js`. In the logged-in Chrome production tab on `/app`, `看东京巴黎悉尼时间` updated the world clock card to 北京、东京、巴黎、悉尼; `汉城和迪拜现在几点` updated it to 北京、首尔、迪拜、洛杉矶. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | This closes a capture-range gap where the widget could display more cities than the shortcut parser understood. The M20 log row is local and should be included with the next grouped commit. |
| 2026-06-17 10:05 CST | M21 weather city command aliases | `看洛杉矶天气`, `LA天气`, `Boston weather` | Expanded weather shortcut city capture to align with the weather widget's supported cities, including 洛杉矶、洛城、LA、los-angeles、波士顿 and Boston, and stripped common command prefixes before the city fallback. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_3Vg4zGp3bJerw1aRzaFeD4DG8UdU` for commit `7868ad7` reached READY and the custom domain served `index-BacGF-zh.js`. In the logged-in Chrome production tab on `/app`, `看洛杉矶天气` switched the weather card to 洛杉矶 and `Boston weather` switched it to 波士顿. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | This closes a daily weather gap where prefixed city commands such as `看洛杉矶天气` could send an unsupported city string. The M21 log row is local and should be included with the next grouped commit. |
| 2026-06-17 10:09 CST | M22 broader market index commands | `看美股三大指数`, `道琼斯怎么样`, `沪深行情` | Expanded market shortcut capture for common index aliases and market shorthand: 美股三大 maps to 标普500、纳斯达克100、道琼斯工业; 沪深 maps to 上证指数、深证成指; and recognized index names can route without requiring explicit `行情` or `指数` wording. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. Vercel production deployment `dpl_EEhGDSqWcKBDiH8hT4uqKu81hgWL` for commit `3092df2` reached READY and the custom domain served `index-B8rxvZVV.js`. In the logged-in Chrome production tab on `/app`, `看美股三大指数` added/updated the market card with 标普500、纳斯达克100、道琼斯工业; `道琼斯怎么样` updated it to 道琼斯工业; and `沪深行情` updated it to 上证指数、深证成指. No `未知工具`, no `我没听懂`, and console error count was 0. | pass | This closes a daily market gap where users naturally name an index or shorthand instead of saying a formal `行情` command. The M22 log row is local and should be included with the next grouped commit. |
| 2026-06-17 10:18 CST | M23 natural converter question commands | `12公里是多少米`, `100华氏是多少摄氏`, `5公斤等于多少磅` | Added converter shortcut parsing for natural unit questions using `是多少`, `等于多少`, and related wording. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetStateActions.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`; commit `d49da28` was pushed to `main`. Production deployment did not advance beyond `dpl_EEhGDSqWcKBDiH8hT4uqKu81hgWL` / `index-B8rxvZVV.js`, and a manual `vercel deploy --prod` retry failed with `api-deployments-free-per-day`. | blocked | Code and tests are ready, but real-page production verification must wait for Vercel deployment quota to reset. The M23 log row is local and should be included with the next grouped commit after production verification. |
| 2026-06-17 10:22 CST | M24 TV channel command aliases | `切到 CCTV13 新闻频道`, `看央视新闻`, `央视五套全屏播放`, `暂停 CCTV1` | Added TV channel alias parsing for CCTV数字、央视/中央数字频道、央视新闻、体育、财经、电影、电视剧、少儿等日常说法. Channel watch/play wording now routes to `tv.play`, switch/change wording routes to `tv.select_channel`, fullscreen wording adds a `tv.fullscreen` follow-up, and pause wording remains on `tv.pause`. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass, but code is intentionally not pushed while Vercel production deployments are blocked by `api-deployments-free-per-day`; real-page verification should run after deployment quota resets. |
| 2026-06-17 10:32 CST | M25 music search/playback control commands | `搜索周杰伦音乐`, `播放周杰伦专辑第一首`, `继续音乐`, `上一首音乐` | Added shortcut routing for music search without autoplay, structured playback preferences for song/album/playlist and result index, `music.previous`, and resume-specific handling so `继续音乐` calls `music.resume` instead of a generic play/search path. The music widget now executes `music.search`, chooses search results by `kind` and `resultIndex`, supports previous-track control, and keeps query state in sync. Verified locally with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts src/widgets/musicKitClient.test.ts src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass, but production real-page verification waits on Vercel deployment quota reset. Full Apple Music account playback still requires a logged-in Apple Music session in the browser. |
| 2026-06-17 10:40 CST | M26 multi-command shortcut execution | `播放 CCTV1 然后暂停电视`, `打开电视同时播放 CCTV1`, `播放 CCTV1 然后做一个很复杂的未知动作` | Added Harness-level segmented shortcut execution for explicit sequential connectors `然后/接着/随后/再` and parallel connectors `同时/与此同时`. Fully local segments execute as shortcut calls, parallel groups emit concurrent running operation events for the status bubble, and partially-unrecognized segmented input falls back without executing the recognized prefix. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/widgetCapabilityBridge.test.ts src/assistant/assistantOperationStatus.test.ts src/assistant/realtimeTextToolCall.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass, but production real-page verification and push are still deferred until Vercel deployment quota recovers. This keeps the batch aligned with the user's request to avoid pushing every small step. |
| 2026-06-17 10:46 CST | M27 sequential add-then-control reuse | `打开音乐然后播放周杰伦音乐` | Improved multi-command shortcut execution so a widget added in one sequential segment can be reused by the next segment before the external context snapshot refreshes. If the second segment was planned as another `board.add_widget` with a widget-detail follow-up for the same definition, the Harness rewrites it to the follow-up tool using the newly added widgetId, preventing duplicate widget windows. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/widgetCapabilityBridge.test.ts src/assistant/assistantOperationStatus.test.ts src/assistant/realtimeTextToolCall.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass. Production real-page verification remains deferred until Vercel deployment quota recovers; this is part of the M24-M27 grouped local batch. |
| 2026-06-17 10:58 CST | M28 close-widget commands are safe window actions | `关闭便签`, `关闭电视和便签`, `取消`, `确认` | Changed `widget.remove` from a destructive/confirmation-required action to a safe close-window action, updated Realtime tool metadata so model/tool selection no longer describes close-widget as requiring confirmation, and kept confirmation coverage on `board.auto_align` plus destructive data actions. Multi-close commands now execute directly in sequence and do not leave a queued confirmation behind. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/realtimeTextToolCall.test.ts src/assistant/openaiRealtimeAdapter.test.ts api/realtime/tool-call.test.ts src/assistant/realtimeSessionConfig.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm --filter @xiaozhuoban/web build`. | blocked | Local command-path tests pass. In-app Browser rendered verification on `http://localhost:5174/app` was blocked because the local tab redirected to `/login` with no local authenticated session. Production real-page verification remains deferred until Vercel deployment quota recovers. |
| 2026-06-17 11:01 CST | M29 noisy close-widget wording | `关闭，啊，这个，音乐`, `把音乐收了`, `音乐收起来` | Expanded close-widget shortcut capture for spoken filler and natural close-window aliases including `收了`, `收起来`, `收一收`, `撤掉`, `拿掉`, and `隐藏`. Alias matching now uses compact normalized text as well as the raw transcript, so punctuation or short pauses between words do not prevent target detection. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/widgetCapabilityBridge.test.ts src/assistant/realtimeTextToolCall.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm --filter @xiaozhuoban/web build`. | blocked | Local routing and command-path tests pass. Production real-page verification remains deferred until Vercel deployment quota recovers; this directly covers the total-acceptance phrase `关闭，啊，这个，音乐` and the learned-command target `把音乐收了`. |
| 2026-06-17 11:08 CST | M24-M29 production real-page verification | `看央视新闻`, `搜索周杰伦音乐`, `打开音乐然后播放周杰伦音乐`, `关闭，啊，这个，音乐`, `关闭音乐和天气`, `上海天气` | Production custom domain advanced to `assets/index-DeNbNSt0.js`. In the logged-in in-app Browser on production `/app`, switched to `工作台测试` and ran the M24-M29 smoke set. `打开音乐然后播放周杰伦音乐` returned `完成：已聚焦小工具；已开始播放音乐`; `关闭，啊，这个，音乐` returned `完成：已删除小工具` with no confirmation; `关闭音乐和天气` completed as `已删除小工具` with no confirmation; `上海天气` restored the weather widget and returned `完成：已切换天气城市`. | partial | Production exposed a remaining TV reliability gap: `看央视新闻` reached the TV tool but returned `没有找到这个电视频道` because the default `Global.m3u` subscription does not include CCTV channels. M30 follows to add a CCTV13 fallback. |
| 2026-06-17 11:13 CST | M30 CCTV news channel fallback | `看央视新闻`, `切到 CCTV13 新闻频道` | Added TV channel normalization for names such as `CCTV13` and `CCTV-13 新闻`, plus a built-in CCTV-13 新闻 HLS fallback used only when the active playlist lacks the requested CCTV news channel. Verified locally with `pnpm --filter @xiaozhuoban/web test -- src/widgets/tvShared.test.ts src/assistant/widgetCapabilityBridge.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. After production advanced to `assets/index-DLgvI0IP.js`, the logged-in in-app Browser on `/app` returned `完成：已选择频道，请手动点击播放` for `看央视新闻`; the page contained `CCTV-13`, did not show `没有找到这个电视频道`, and did not show `未知工具`. | pass | This closes the production TV gap found in the M24-M29 smoke test. The fallback HLS endpoint returned HTTP 200 with `application/vnd.apple.mpegurl` and CORS enabled during verification. |
| 2026-06-17 11:28 CST | M31 sequential music search then play first | `先打开音乐，再搜索七里香，然后播放第一首` | Added planning-context carryover for sequential shortcut groups: a planned `board.add_widget` or `widget.focus` updates the later planning context with the focused widget type, and widget-detail calls against a planned widget are rewritten to the real widget id after creation. Also allowed implicit music search commands such as `搜索七里香` when music is the focused widget. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/widgetCapabilityBridge.test.ts src/assistant/realtimeTextToolCall.test.ts src/widgets/musicKitClient.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass and prove the three segments run as `board.add_widget`, `music.search`, then `music.play` against the same newly added music widget. Production real-page verification should run after deploy. |
| 2026-06-17 11:34 CST | M32 confirmation queue continues to todo | `清空剪贴板，然后添加一条待办：明天买牛奶`, `确认` | Added acceptance coverage for a destructive clipboard clear followed by a queued todo creation. The first command returns `needs_confirmation` for `clipboard.clear`; after `确认`, the Harness executes the pending clear and then resumes the queued `todo.add_item`, creating `明天买牛奶` without model fallback. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web build`, and `pnpm test`. | blocked | Local tests pass. Production real-page verification should run after deploy; use a temporary clipboard entry and remove the temporary todo afterward if needed. |
| 2026-06-17 11:40 CST | M33 close-widget confirmation policy for Realtime | `关闭小工具`, `关闭音乐`, `关闭电视和便签` | Clarified both shared Realtime session instructions and the Vercel session API instructions: close/收起 widget windows must call `widget.remove` without confirmation, while clearing content, deleting user data, overwriting content, and bulk data mutations still require confirmation. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/realtimeSessionConfig.test.ts api/realtime/session.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/assistantAcceptance.test.ts`, and `pnpm --filter @xiaozhuoban/web typecheck`. | pass | This preserves the existing local `widget.remove` safe execution path and removes the Realtime prompt ambiguity that could make the model ask for confirmation when the user only wants to close a widget window. |
| 2026-06-17 11:49 CST | M34 simultaneous daily utility command | `打开音乐，同时查北京天气` | Added local Harness coverage for simultaneous shortcut groups that add/focus one widget while adding another widget and running a safe follow-up. The command executes as two parallel `board.add_widget` calls, then continues the weather branch with `weather.set_city` against the newly added weather widget. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:49 CST | M35 casual music plus countdown command | `帮我放点轻松的音乐，然后把倒计时设为 10 分钟` | Expanded music query cleanup for casual spoken playback fillers such as `帮我放点...音乐`, so the search/play query becomes `轻松` instead of `放点轻松的`. Added local Harness coverage proving the full sequential command executes as add music + `music.play`, then add countdown + `countdown.set`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:53 CST | M36 simultaneous music pause and news open | `暂停音乐，同时打开新闻` | Added local Harness coverage for a common mixed command where an existing music widget is controlled while a headline widget is opened in the same parallel group. The command executes as `music.pause` against the existing music widget plus `board.add_widget` for news, and emits concurrent running operation events for both branches. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:53 CST | M37 sequential weather and world-clock setup | `打开天气查北京，再打开世界时钟看东京时间` | Added local Harness coverage for sequential information-widget setup. The weather segment adds the weather widget then runs `weather.set_city` with `北京`; the world-clock segment adds the world clock widget then runs `worldClock.set_zones` for Tokyo. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:57 CST | M38 casual note shorthand | `帮我记一下今天继续测试小桌板` | Added shortcut routing for casual note-taking phrases such as `记一下`, `记下`, `记录一下`, `写一下`, and `记一笔` when the command does not explicitly target todos, reminders, clipboard, or message board. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:57 CST | M39 clipboard copy wording | `复制账号 demo 到剪贴板` | Expanded clipboard add routing and text extraction for `复制`, `拷贝`, `存一下`, and `存` wording, including phrases where the copied text appears before `到剪贴板`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 11:57 CST | M40 casual translate shorthand | `翻译一下 hello`, `翻译一下 你好` | Added translation routing for `翻译一下 ...` without an explicit target language. Latin text defaults to Chinese and Chinese text defaults to English, while explicit targets such as `翻译成英文` continue to win. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:00 CST | M41 natural Chinese arithmetic | `12加30是多少`, `12乘以8` | Expanded calculator routing to recognize arithmetic expressions that use Chinese operator words such as `加`, `减`, `乘以`, and `除以`, while preserving existing symbolic expressions like `12+30`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:00 CST | M42 natural headline requests without TV collision | `今天有什么新闻`, `看央视新闻` | Expanded headline refresh routing for natural requests such as `今天有什么新闻`, `看看新闻`, and `最新头条`, and added a guard so CCTV/央视频道 phrases such as `看央视新闻` continue to route to TV playback instead of headline refresh. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:03 CST | M43 call-me reminder wording | `下午三点叫我开会` | Expanded todo/reminder routing so `叫我` and `到点叫我` create todo reminders with parsed due times, matching existing `提醒我` behavior. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:03 CST | M44 relative reminder time | `十分钟后叫我喝水` | Added relative due-time parsing for reminder phrases using hour/minute/second durations followed by `后`, and strips the relative time from the resulting todo text. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:03 CST | M45 timer shorthand for countdown | `定时十分钟` | Expanded countdown duration routing so timer shorthand words such as `定时` and `计时` set/start the countdown widget without requiring the user to say `倒计时`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:06 CST | M46 clear-note shorthand | `清一下便签` | Expanded note-clear routing for shorthand such as `清一下便签`, `清掉便签`, and `清除一下便签`, preserving the split between clearing note content and closing the note widget. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:06 CST | M47 clear-clipboard shorthand | `清一下剪贴板` | Expanded clipboard-clear routing for shorthand such as `清一下剪贴板`, `清掉剪贴板`, and `清除一下剪贴板`, keeping the action on `clipboard.clear` rather than closing the clipboard widget. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:06 CST | M48 todo completion shorthand | `把买牛奶勾掉` | Expanded todo completion routing so obvious completion wording such as `勾掉`, `勾选`, `做完`, and `标记完成` can omit the word `待办` when an item is named and no conflicting widget target is present. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. |
| 2026-06-17 12:10 CST | M49-M51 query alias batch | `帝都天气`, `魔都天气`, `美股怎么样`, `A股行情`, `NYC and Tokyo time` | Expanded daily information-widget shortcut capture for common city nicknames, broad market-region questions, and English world-clock aliases. Weather nicknames such as 帝都、魔都、羊城、鹏城、杭城、蓉城、山城 map to frontend-supported cities; broad market phrases such as 美股怎么样 and A股行情 select the expected index groups; English world-clock phrasing such as `NYC and Tokyo time` routes to worldClock.set_zones without model fallback. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 12:14 CST | M52-M54 casual utility command batch | `暂停计时`, `继续定时器`, `重置定时`, `帮我记个便签：晚上复盘`, `hello 是什么意思` | Expanded local shortcut capture for casual countdown control, note writing, and meaning-question translation. Countdown control now accepts `计时`/`定时器`/`定时` wording without closing the widget; note writing accepts `记个便签` style commands; and meaning questions such as `hello 是什么意思` set the translate widget to Chinese without model fallback. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 12:17 CST | M55-M56 open/close widget alias batch | `唤出清单`, `调出播放器`, `开一下单位换算`, `关掉复制板` | Expanded local open-widget intent capture for casual verbs such as `开一下`, `叫一下`, `唤出`, `调出`, and `拉起`, and widened daily widget aliases such as 清单、单位换算、复制板、电视机、音乐播放器、世界时间、翻译器、录音机、消息板. Matching now also uses compact normalized text so punctuation and spacing are less brittle. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 12:22 CST | M57-M59 board, clipboard, message shortcuts | `回默认`, `回我的`, `固定保存到剪贴板账号是 demo`, `给大家说一声今天下午三点开会` | Expanded board switching to support known board-name short phrases such as `回默认` and `回我的`; added pinned clipboard save parsing so fixed/置顶 clipboard commands pass `pinned: true`; and expanded message-board capture for casual announcement wording such as `给大家说一声...`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. Message-board execution remains covered through its capability bridge path; this milestone adds shortcut routing coverage for the natural wording. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 12:25 CST | M60-M61 recorder and dial-clock control aliases | `开始录制`, `播放录制`, `暂停录制`, `退出夜灯`, `打开深色模式` | Expanded recorder routing so `录制` wording is handled the same as `录音`, including start/play/pause commands, and expanded dial-clock night-mode wording to include 夜灯、暗色模式、深色模式 plus off wording such as `退出夜灯`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/widgetCapabilityBridge.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 12:30 CST | M62-M63 soon reminders and everyday weight units | `一会儿提醒我喝水`, `2斤是多少克` | Added a deterministic shortcut `currentTime` from the Harness clock so relative reminders use the test/app clock instead of wall-clock time, mapped `一会儿/待会儿/等会儿` reminders to 10 minutes later, and added everyday Chinese weight input aliases for conversion from `斤`/`两` into supported base units. For `2斤是多少克`, the router scales the value to `1 kg -> g` instead of incorrectly treating `斤` as `kg`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test -- src/index.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only per user request; no deployment or production page verification was performed. Reverse conversions such as `公斤是多少斤` still fall through rather than forcing an unsupported display unit. The build still reports the known nonfatal Vite chunk warning for `vendor-hls.js` over 500 kB. |
| 2026-06-17 14:44 CST | Widget assistant module Realtime plan Milestone 0-2 | Realtime API auth, two-stage Realtime selection, WebRTC session readiness | Implemented the local-only Milestone 0-2 slice from `docs/widget-assistant-module-realtime-learning-plan.md`: froze assistant responsibility boundaries and `CommandPlan` skeleton in `assistant-core`; moved shared Realtime model/instructions/turn-detection/tool-selector constants into reusable config; changed text fallback default to `gpt-4.1-mini` rather than `gpt-realtime-2`; required Supabase Bearer auth for `/api/realtime/session` and `/api/realtime/tool-call`; derived OpenAI Safety Identifier from the authenticated Supabase user id; sent browser Supabase access tokens from the Realtime adapter; gated WebRTC tool handling on `session.updated`; added `configuring`/`session_failed` UI states; and prevented unconditional `response.create` while a Realtime response is active. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm test`. | pass | Local-only per user request; no deployment and no push were performed. Existing shortcut commands were preserved; no shortcut conflict was found in this slice. The two-stage selector/scoped-context tests remain in place and assert first-pass catalog-only behavior plus second-pass scoped context. |
| 2026-06-17 15:29 CST | Widget assistant module Realtime plan Milestone 3-5 | Registry, CommandPlan validation, Supabase/local audit | Implemented the local-only Milestone 3-5 slice from `docs/widget-assistant-module-realtime-learning-plan.md`: added `WidgetAssistantModule` and `WidgetAssistantRegistry` in `assistant-core`; added registry catalog generation and selected-module scoped context; wired daily-widget modules through an independent compatibility module layer that reuses existing actions and shortcut metadata; kept Realtime-2 two-stage parsing so first pass receives only the module/tool catalog and second pass receives only the selected module scoped context; added `CommandPlan` creation plus `PlanValidator` enforcement before harness execution so unknown tools, disabled modules, tool/module mismatch, extra fields, and invalid arguments fail locally; expanded audit events/log rows with operation id, normalized input, candidate modules, selected module/tool hint, and confidence; and routed logged-in users to Supabase audit with local audit fallback for anonymous/local/error cases. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm test`. | pass | Local-only per user request; no deployment, no push, and no staging were performed. Existing shortcut commands and shortcut-first routing were preserved; no conflict was found, so no shortcut behavior was removed. The module layer is deliberately per-widget and per-tool bounded to prepare for dynamic registration, disabling, uninstalling, and future hot loading. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 6 | Capability schema tightening and preview gate | Added strict object schemas in `assistant-core`, module-level action specs, result schema metadata, argument key lists, and `PlanPreview`. Daily widget module actions now wrap legacy actions with module-scoped minimal schemas; examples include `music.pause` accepting only `widgetId`, `messageBoard`-style text actions requiring text when present, and `tv.select_channel` accepting only `widgetId`, `channelName`, and `channelUrl`. Harness confirmation now attaches a structured preview for confirm/destructive plans. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only; no deploy or push. Existing shortcut semantics were preserved. No shortcut conflict was found; regression coverage includes `music.pause` rejecting unrelated `query` and clipboard clear remaining high risk. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 7 | CommandExecutor, concurrency, failure propagation, status bubble | Added `CommandExecutor` with sequential/parallel groups, dependency skip behavior, per-command operation events, and failure isolation. The existing Harness status-bubble event path remains active, and the Dock now also shows low-cost runtime/outbox status without expanding layout. Verified with core executor tests plus existing Harness concurrent shortcut tests through `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and web build. | pass | Local-only. This keeps the existing shortcut group execution path stable while adding a reusable executor for future full CommandPlan execution. No old shortcut was removed. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 8-9 | Music pilot and first-batch daily module migration | Added independent module directories and module docs/test-case assets for `music`, `weather`, `clipboard`, `todo`, `translate`, `calculator`, `countdown`, `worldClock`, `market`, `headline`, `recorder`, and `tv`. Added module-level aliases, shortcuts, tools, scoped context, execution policy, action specs, migration notes, and test matrix metadata through the compatibility factory. Music regression explicitly preserves `关闭音乐` as window close and `暂停音乐` as playback pause. Verified all registered first-batch modules expose catalog/scoped context and pass static module runner checks. | pass | Internal actions still use a deliberate `legacyBridge: true` adapter where the old ActionRegistry/capability bridge owns execution. Module boundaries, schemas, context whitelists, and test matrices are now independent so tools can be disabled/unregistered or migrated one by one. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 10 | Low-cost 24-hour local standby mode | Added `RealtimeRuntimeController`, budget metrics, soft/hard limit gates, local standby default, manual Realtime connection gate, active-time accounting, and Dock runtime status text. App now displays runtime mode, Realtime active seconds, estimated cost, and continues to run text/local shortcut commands without creating Realtime sessions. Verified soft/hard limit behavior in core tests and UI formatting in Dock tests. | pass | Local-only. No background microphone/VAD implementation was added in this slice; the state machine and budget gate are ready for a future wake detector. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 11 | Cloud write outbox | Added localStorage-backed assistant mutation outbox for common widget cloud writes: `widget.upsert`, `widget.upsert_many`, and `widget.delete`. Store optimistic UI writes now enqueue failed background persistence mutations; Dock shows pending sync count and exposes retry through the current repository. Verified enqueue/retry behavior with `apps/web/src/assistant/assistantOutbox.test.ts` and full test suite. | pass | Local-only; no Supabase deployment or migration execution. This covers high-frequency widget mutations used by assistant tools; broader board/definition/import writes remain future expansion. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 12-13 | Learning system and AI-generated module review/hot-load gate | Added data-layer learning primitives: candidate creation, policy, in-memory learned store, conflict rejection, confirmation state, and regression-case payloads. Audit rows now include `learning_candidate` so successful model/function-call fallbacks can be reviewed. Added AI generated module review and install gate that rejects tool-name conflicts, alias conflicts, missing schemas, unsafe logic kinds, and requires explicit confirmation before registry install. Verified with core tests and Supabase schema file updates. | pass | Learning rules are stored as data candidates, not source-code edits. No automatic install or shortcut overwrite is allowed. Existing no-conflict shortcuts remain unchanged. |
| 2026-06-17 16:37 CST | Widget assistant module Realtime plan Milestone 14 | Local real-page total acceptance attempt | Started local dev server; Vite selected `http://localhost:5175/`. Playwright opened `http://localhost:5175/app`, but the app redirected to `http://localhost:5175/login`. Snapshot showed only login controls (`登录小桌板`, email, password, disabled login button), so the 11-command real app acceptance set could not be run without a local authenticated session. | blocked | This is a local auth/session blocker, not a build or routing failure. No deployment was performed, so production real-page verification was intentionally not used for this local-only round. |
| 2026-06-17 19:14 CST | Execution plan v2 Batch 1 Milestone A | Module protocol gap fixes | Added registry APIs to list active module tools, shortcuts, and test matrices; added `ModuleMigrationReport` and `ShortcutConflictReport` structures; tightened module static validation so missing action specs, argument keys, result schema, examples, `redactContext`, and `maxRealtimeContextTokens` fail module checks. Verified active/disabled module listing and stricter static checks with `pnpm --filter @xiaozhuoban/assistant-core test` and `pnpm --filter @xiaozhuoban/web test -- src/widgets/modules/dailyWidgetAssistantModules.test.ts`. | pass | Existing shortcuts were not changed. No conflict found; no shortcut removal. |
| 2026-06-17 19:14 CST | Execution plan v2 Batch 1 Milestone D | CommandPlan main execution chain | Added `ShortcutPlanAdapter`, `RealtimePlanAdapter`, and `TextFallbackPlanAdapter`. Updated Harness non-confirmation tool execution so shortcut/model/function-call paths are converted into `CommandPlan`, validated with `PlanValidator`, executed by `CommandExecutor`, emitted through operation events, then audited and sent back through Realtime result handling. Verified with Harness and acceptance tests plus full `pnpm test`. | pass | `assistant.confirm` and `assistant.cancel` remain local confirmation controls; model/tool outputs still go through plan validation before execution. |
| 2026-06-17 19:14 CST | Execution plan v2 Batch 1 Milestone E | Preview gate and confirmation UI | Extended confirmation requests with structured `PlanPreview` and rendered preview lines in the visible Dock confirmation area, including command list, impact/reversibility, recovery strategy, and confirm/cancel buttons. Added UI helper tests for preview text. Local page validation attempted at `http://localhost:5175/app`, but redirected to `/login`, so real UI flow remains blocked by missing local authenticated session. Verified build with `pnpm --filter @xiaozhuoban/web build`. | partial | Unit/integration/build checks pass. Real page preview UI verification is blocked by local login state and is not marked complete. |
| 2026-06-17 19:22 CST | Execution plan v2 Batch 2 Milestone B | Remove music from central daily module factory | Moved music module ownership out of `dailyWidgetAssistantModules`: `createLocalAssistantHarness` now registers `createMusicAssistantModule` from `modules/music`, and the central factory no longer includes the music seed, music tool arg schemas, or music examples. Added a regression test that the central factory no longer emits `music` while the full registry still includes music plus the remaining daily modules. Verified with targeted module/Harness/Realtime tests, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcuts were preserved. No shortcut conflict was found; `musicShortcutConflictReport.resolution` remains `none`. Remaining non-music modules still use the compatibility factory pending later batches. |
| 2026-06-17 19:22 CST | Execution plan v2 Batch 2 Milestone C | Music complete pilot module boundary | Added independent music files for definition, shortcuts export, tools, scoped context, realtime catalog, execution policy, and assistant assembly. Music strict schemas reject extra fields such as `query` on `music.pause`; Realtime first stage catalog stays free of widget ids/private summaries; selected-module scoped context only includes music instances and strips clipboard/private data. `关闭音乐` remains `widget.remove`; `暂停音乐` remains `music.pause`. Verified with `apps/web/src/widgets/modules/dailyWidgetAssistantModules.test.ts`, Realtime adapter/text tests, Harness tests, full web tests, core tests, monorepo tests, typecheck, and build. | pass | Execution still deliberately uses `legacyBridge: true` at the capability bridge layer while module metadata/policy/schema/context are now owned by `modules/music`. MusicKit-specific low-level failure handling remains a later execution-layer extraction item, recorded in `musicMigrationReport.pendingItems`. |
| 2026-06-17 19:22 CST | Execution plan v2 Batch 2 local page attempt | `http://localhost:5175/app` | Started local Vite dev server and opened `/app` with Playwright CLI. The app redirected to `http://localhost:5175/login`; snapshot showed only login controls (`登录小桌板`, email, password, disabled login button). No authenticated local session was available, so real-page command execution for the music module could not be exercised in this local-only run. | blocked | This repeats the known local auth/session blocker. No deployment or production verification was performed. |
| 2026-06-17 19:30 CST | Execution plan v2 Batch 2 Milestone B/F/G | Weather, clipboard, and todo module migration | Moved `weather`, `clipboard`, and `todo` out of `dailyWidgetAssistantModules` into independent module directories with `definition.ts`, `shortcuts.ts`, `tools.ts`, `context.ts`, `realtime.ts`, `executionPolicy.ts`, and real `assistant.ts` assembly. `createLocalAssistantHarness` now registers these modules directly before the remaining compatibility factory modules. The central factory no longer emits `music`, `weather`, `clipboard`, or `todo`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/widgets/modules/dailyWidgetAssistantModules.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcuts were preserved: weather city aliases, clipboard save/clear aliases, and todo reminder/completion aliases. No shortcut conflict was found for all three modules. |
| 2026-06-17 19:30 CST | Execution plan v2 Batch 2 context/privacy regression | Weather/clipboard/todo scoped context | Added module regression coverage proving selected-module strict schemas reject extra model fields on weather and music, and proving clipboard/todo scoped contexts redact content-heavy summaries. Clipboard scoped context now sends `clipboard-content-redacted` plus count/pinned hints only; todo scoped context sends `todo-summary-only` plus count when inferable. Realtime first-stage catalog remains free of widget ids and private summaries. | pass | This advances Milestone F/G privacy constraints for the migrated tools; broader Realtime audio fallback remains unverified because local real-page auth is blocked. |
| 2026-06-17 19:30 CST | Execution plan v2 Batch 2 local page attempt | `http://localhost:5175/app` | Started local Vite dev server and opened `/app` with Playwright CLI. The app again redirected to `http://localhost:5175/login`; snapshot showed only login controls (`登录小桌板`, email, password, disabled login button). No authenticated local session was available, so real-page command execution for weather/clipboard/todo could not be exercised locally. | blocked | This is the same local auth/session blocker as earlier. No deployment or production verification was performed. |
| 2026-06-17 19:38 CST | Execution plan v2 Batch 2 Milestone B/F/G | Countdown, worldClock, and headline module migration | Moved `countdown`, `worldClock`, and `headline` out of `dailyWidgetAssistantModules` into independent module directories with `definition.ts`, `shortcuts.ts`, `tools.ts`, `context.ts`, `realtime.ts`, `executionPolicy.ts`, and real `assistant.ts` assembly. `createLocalAssistantHarness` now registers these modules directly before the remaining compatibility factory modules. The central factory no longer emits `music`, `weather`, `clipboard`, `todo`, `countdown`, `worldClock`, or `headline`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/widgets/modules/dailyWidgetAssistantModules.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcuts were preserved: countdown set/control aliases, world-clock city/time aliases, and headline/news aliases. No shortcut conflict was found for all three modules. |
| 2026-06-17 19:38 CST | Execution plan v2 Batch 2 context/privacy regression | Countdown/worldClock/headline scoped context | Added module regression coverage proving selected-module strict schemas reject extra model fields on countdown, worldClock, and headline, and proving scoped contexts redact sensitive summaries. Countdown scoped context now sends `countdown-state-summary`; worldClock sends selected zones only and no location tracking; headline sends `headline-metadata-only` and never full article text or reading history. | pass | This advances Milestone F/G privacy constraints for migrated information/task tools. Realtime audio fallback and real-page command evidence remain blocked by local auth. |
| 2026-06-17 19:38 CST | Execution plan v2 Batch 2 local page attempt | `http://localhost:5175/app` | Started local Vite dev server and opened `/app` with Playwright CLI. The app again redirected to `http://localhost:5175/login`; snapshot showed only login controls (`登录小桌板`, email, password, disabled login button). No authenticated local session was available, so real-page command execution for countdown/worldClock/headline could not be exercised locally. | blocked | This is the same local auth/session blocker as earlier. No deployment or production verification was performed. |
| 2026-06-17 19:46 CST | Execution plan v2 Batch 2 Milestone B/F/G | Market, calculator, and translate module migration | Moved `market`, `calculator`, and `translate` out of `dailyWidgetAssistantModules` into independent module directories with `definition.ts`, `shortcuts.ts`, `tools.ts`, `context.ts`, `realtime.ts`, `executionPolicy.ts`, and real `assistant.ts` assembly. `createLocalAssistantHarness` now registers these modules directly before the remaining compatibility factory modules. The central factory no longer emits `music`, `weather`, `clipboard`, `todo`, `countdown`, `worldClock`, `headline`, `market`, `calculator`, or `translate`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/widgets/modules/dailyWidgetAssistantModules.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcuts were preserved: market region/index aliases, calculator arithmetic/unit aliases, and translate shorthand. No shortcut conflict was found for all three modules. |
| 2026-06-17 19:46 CST | Execution plan v2 Batch 2 context/privacy regression | Market/calculator/translate scoped context | Added module regression coverage proving selected-module strict schemas reject extra model fields on market, calculator, and translate, and proving scoped contexts redact sensitive summaries. Market scoped context now sends index-group summary only and explicitly disallows advice/trading; calculator sends current display summary and marks local arithmetic preferred; translate sends draft metadata only and does not include long/private source text by default. | pass | This advances Milestone F/G privacy constraints for migrated information tools. Realtime audio fallback and real-page command evidence remain blocked by local auth. |
| 2026-06-17 19:46 CST | Execution plan v2 Batch 2 local page attempt | `http://localhost:5175/app` | Started local Vite dev server and opened `/app` with Playwright CLI. The app again redirected to `http://localhost:5175/login`; snapshot showed only login controls (`登录小桌板`, email, password, disabled login button). No authenticated local session was available, so real-page command execution for market/calculator/translate could not be exercised locally. | blocked | This is the same local auth/session blocker as earlier. No deployment or production verification was performed. |
| 2026-06-17 19:54 CST | Execution plan v2 Batch 2 Milestone B/F/G | Recorder and TV module migration | Moved `recorder` and `tv` out of `dailyWidgetAssistantModules` into independent module directories with `definition.ts`, `shortcuts.ts`, `tools.ts`, `context.ts`, `realtime.ts`, `executionPolicy.ts`, and real `assistant.ts` assembly. `createLocalAssistantHarness` now registers both modules directly before the remaining compatibility factory modules. The central factory no longer emits any of the 12 first-batch daily modules and only retains `note` compatibility ownership. Verified with targeted module/Harness/acceptance tests, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcuts were preserved: recorder start/stop/play/pause aliases and TV play/pause/channel/fullscreen aliases. No shortcut conflict was found for either module. |
| 2026-06-17 19:54 CST | Execution plan v2 Batch 2 context/privacy regression | Recorder/TV scoped context | Added module regression coverage proving selected-module strict schemas reject extra model fields on `recorder.play` and `tv.select_channel`, and proving scoped contexts redact sensitive summaries. Recorder scoped context sends recording state and permission hints only, never recording content; TV scoped context sends playback/channel summary only, never playlist/private source URLs. | pass | This completes the first-batch migrated-module context/privacy regression set while keeping Realtime first-stage catalog free of widget ids and private summaries. |
| 2026-06-17 19:54 CST | Execution plan v2 Batch 2 local page attempt | `http://localhost:5175/app` | Started local Vite dev server and opened `/app` with Playwright CLI. The app again redirected to `http://localhost:5175/login`; snapshot showed only login controls (`登录小桌板`, email, password, disabled login button). No authenticated local session was available, so real-page command execution for recorder/TV could not be exercised locally. | blocked | This is the same local auth/session blocker as earlier. No deployment or production verification was performed. |
| 2026-06-17 20:01 CST | Execution plan v2 Batch 3 Milestone I | Outbox critical-write coverage and visible retry failure | Expanded assistant outbox mutations beyond widget instance writes to include `board.upsert`, `board.delete`, `widget_definition.upsert`, `widget_definition.upsert_many`, and `backup.import`. Store board changes, AI-generated widget definitions, and backup imports now keep optimistic local UI while enqueuing failed persistence writes for retry. Dock runtime status now restores pending count after refresh and shows the latest retry failure reason next to the retry button. Verified with targeted outbox/Dock/store tests, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only; no deployment. UI-originated store writes still have no command operation id by design; assistant-originated store writes are covered in the 20:08 row. |
| 2026-06-17 20:08 CST | Execution plan v2 Batch 3 Milestone I | Command operation id to outbox linkage | Added `operationId` to `AssistantActionContext`, passed Harness tool call ids into registry execution, and propagated command operation ids through board and widget-detail action stores into store persistence options. Failed assistant-originated widget writes now enqueue outbox mutations with the originating command id, so audit `operation_id` can be matched to the outbox mutation `operationId`. Verified with board action, widget state action, and store/outbox regression tests plus `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcut semantics were preserved: tests assert ordinary board calls do not receive extra undefined options, while command-context calls do receive operation ids. |
| 2026-06-17 20:19 CST | Execution plan v2 Batch 3 Milestone K | Learning candidate confirmation and learned local hit | Added localStorage-backed `LearnedCommandStore` wiring, pending learning confirmation through the existing Dock confirmation controls, and learned shortcut matching before shortcut/model fallback. Successful model/function-call plans can now create non-sensitive learning candidates; user confirmation stores the rule; rejection prevents local hits; confirmed learned commands still run through `CommandPlan -> PlanValidator -> CommandExecutor -> audit`. Verified with core learning tests, Harness learned-route tests, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web test`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only; no deployment. Sensitive payloads such as password/API key text are blocked from candidate creation. Existing shortcuts still win before fallback, so already-supported phrases are not relearned unnecessarily. |
| 2026-06-17 20:27 CST | Execution plan v2 Batch 3 Milestone L | AI generated module review, sandbox, and dynamic registry gate | Tightened AI generated module review into a local-only install session: `parseAiGeneratedModuleManifest` now rejects malformed manifests and undeclared code fields; review requires tool names to stay inside the generated module namespace, rejects existing tool/alias conflicts including disabled modules, requires minimal object schemas with `additionalProperties: false`, and blocks unsafe logic payloads. `runAiModuleSandboxTests` validates minimal valid args and extra-field rejection before confirmation; `installReviewedModule` can require a passing sandbox plus explicit user confirmation before registering the module. Regression tests prove unconfirmed installs cannot alter catalog/shortcuts, sandbox failure blocks install, confirmed install appears in registry, and disable/unregister immediately removes catalog and shortcuts. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web test`, and `pnpm test`. | pass | Local-only; no deployment and no real AI-generated module was installed. This preserves existing no-conflict shortcuts and keeps generated modules constrained to manifest/logic specs rather than arbitrary React/source code. |
| 2026-06-17 20:34 CST | Execution plan v2 Batch 3 Milestone J | Low-cost 24 hour runtime mode closure | Extended `RealtimeRuntimeController` with local wake detection, explicit standby elapsed tracking, fallback/local-hit metrics, cooldown transition, and hard-limit manual override semantics. `createRealtimeAssistantRuntime` now exposes wake-connect, idle handling, route accounting, injectable budget config, and an adapter factory for deterministic tests; connected command/dialogue windows schedule idle timeout disconnects. `VoiceAssistantDock` reports completed command routes back to the runtime so local shortcut/learned hits increase local metrics without Realtime cost. Regression tests prove 24 hours of local standby creates no Realtime session, command-window idle disconnects automatically, hard limit blocks automatic wake Realtime while manual continuation remains possible, and local/model routes update metrics without adding cost. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, targeted runtime/Dock tests, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web test`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Local-only; no deployment. Build passes with the existing large chunk warning for `App` and `vendor-hls`. Real microphone/VAD audio capture remains environment-dependent; this batch adds the local wake/runtime policy hooks and deterministic idle/budget tests. |
| 2026-06-17 20:41 CST | Execution plan v2 Batch 4 Milestone M | Executable per-tool test matrix coverage | Expanded `WidgetModuleTestCase` with coverage categories, covered actions/risks/context fields, and failure regression-candidate metadata. `runWidgetModuleStaticChecks` now reports uncovered actions, risks, scoped context fields, missing coverage categories, and regression candidates. Extended all 12 migrated daily-tool `test-cases.json` files to cover single-tool commands, noisy speech, missing-arg/failure cases, Realtime fallback, cross-tool combinations, window controls, permission/auth or mounted capability cases, learning regression, scoped-context redaction, and audit redaction. Added a module test that loads every JSON matrix and fails with the module name if any action, risk, context field, or required category is missing; music/recorder/tv additionally require mounted capability coverage. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test -- src/widgets/modules/dailyWidgetAssistantModules.test.ts`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm --filter @xiaozhuoban/web test`, and `pnpm test`. | pass | Local-only; no deployment. Matrices are executable coverage metadata plus command examples; Milestone N real-page DOM/screenshot evidence remains blocked until an authenticated local session is available. |
| 2026-06-17 20:45 CST | Execution plan v2 Batch 4 Milestone N | Local real-page total acceptance attempt | Started local Vite dev server at `http://localhost:5173/` and opened `http://localhost:5173/app` with Playwright CLI. The app redirected to `http://localhost:5173/login`, proving no authenticated local browser session was available. Snapshot DOM evidence: heading `登录小桌板`, textbox `邮箱`, textbox `密码`, disabled button `登录`, and link `还没有账号？去注册`. Because the page never reached `/app`, the 11-command total acceptance set could not be executed and no command-level normalized text, segments, CommandPlan, status bubble, audit/outbox, learning candidate, or per-tool DOM evidence could be collected. | blocked | This satisfies the plan's blocker recording rule for missing local login state and does not mark Milestone N as pass. No deployment or production verification was performed. Supabase/OpenAI env keys exist locally, but there is no usable authenticated browser session. |
| 2026-06-17 21:23 CST | Execution plan v2 Realtime-2 CommandPlan fallback correction | `打开音乐，播放周杰伦`, `打开音乐，同时查北京天气`, `开始工作` | Removed the attempted direction of hard-coding complex/macro commands into the local harness and added a Realtime-2-style CommandPlan fallback path instead. `AssistantRealtimeAdapter` can now return `CommandPlan`; Harness executes model plans only through `CommandPlan -> PlanValidator -> CommandExecutor -> audit/tool-result`. `/api/realtime/tool-call` now supports `plan_select` without desktop context and `plan_execute` with selected module scoped contexts, returning a harness-validated multi-tool plan with sequential/parallel execution groups. Adapter tests assert first-pass catalog-only behavior and second-pass `moduleContexts`; Harness tests assert parallel model plans execute through operation events; API tests assert plan selection/execution payloads do not leak unrelated note summaries. Also fixed a deterministic shortcut conflict where `关闭天气` could be routed as a weather city query instead of `widget.remove`. Verified with `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test`, `pnpm --filter @xiaozhuoban/web typecheck`, `pnpm test`, and `pnpm --filter @xiaozhuoban/web build`. | pass | Existing shortcut commands were preserved; no new local macro shortcut was retained. Complex commands should now be owned by Realtime-2 plan fallback instead of expanding harness regex coverage. |
| 2026-06-17 21:23 CST | Execution plan v2 Realtime-2 local connection attempt | `连接语音` | After logging into the local dev page and reaching `/app`, Playwright clicked `连接语音`. The visible assistant state changed from `未连接 / 本地待机 · Realtime 按需连接` to `有错误 / 没有检测到可用麦克风，或当前浏览器不支持录音。 / 失败：没有检测到可用麦克风，或当前浏览器不支持录音。 / realtime_dialogue_window · Realtime 0s · $0.0000`. This means the local in-app browser environment did not reach the Realtime session/API stage. | blocked | This is a real-page environment blocker for live WebRTC Realtime-2 validation. The local environment also has no usable server OpenAI key configured, so live model-backed plan execution must be rerun in an environment with microphone support and `OPENAI_API_KEY`. No deployment was performed. |
| 2026-06-17 21:37 CST | Execution plan v2 Realtime-2 local dev API unblock | `连接语音`, `/api/realtime/session`, `/api/realtime/tool-call` | After the microphone was connected, browser permission reached `granted` and the on-page `连接语音` flow progressed past the previous microphone-only gate. Local Vite dev initially returned `404` for `/api/realtime/session`, so a dev-only Vite middleware now serves `/api/realtime/session` and `/api/realtime/tool-call` via the existing API handlers and loads the same local env keys those handlers expect. Curl verification on `http://localhost:5173` returned `401 {"error":"AUTH_REQUIRED"}` for both endpoints without a token, and `401 {"error":"AUTH_INVALID"}` for session with a fake bearer token, proving the local API route is reachable and no longer fails with `SUPABASE_SERVER_CONFIG_MISSING`. | partial | This removes the local Vite routing/config blocker for Realtime validation. Live WebRTC/model execution is still not marked pass: the local `OPENAI_API_KEY` is empty, and Chrome plugin control could not communicate with the enabled Codex Chrome Extension until the user confirms opening a Chrome window for reconnection. No deployment was performed. |
| 2026-06-17 21:40 CST | Execution plan v2 Chrome-plugin Realtime validation attempt | Chrome extension control | After the Chrome plugin was opened, retried the Chrome extension backend and still received `Browser is not available: extension`. Followed the plugin recovery flow: verified Chrome was running, the Codex Chrome Extension was installed and enabled in the selected `Default` profile, and the native host manifest was correct; then opened a Chrome window for the selected profile and retried after 2 seconds. The retry still returned `Browser is not available: extension`, so Chrome-driven real-page Realtime validation could not proceed in this session. | blocked | This is a Chrome plugin communication blocker, not an app regression. The local Realtime API routing/config unblock above remains valid. No deployment was performed. |
| 2026-06-17 21:50 CST | Execution plan v2 Milestone N local real-page acceptance retry | 11-command Milestone N set | Logged into local `http://localhost:5173/app`, switched to `工作台测试`, and ran the full Milestone N command set through the visible assistant text input. Passing/working evidence: `打开音乐，播放周杰伦` mounted `音乐播放器`; `先打开音乐，再搜索七里香，然后播放第一首` returned `已聚焦小工具；已搜索音乐；已开始播放音乐` and the music widget showed `Qi-Li-Xiang` results; `打开音乐，同时查北京天气` returned music focus plus Beijing weather; `关闭音乐和天气` removed both widgets without confirmation; `清空剪贴板，然后添加一条待办：明天买牛奶` showed preview lines for `clipboard / clipboard.clear`, required dock-scoped confirmation, then returned `已清理剪贴板历史；已新增待办` and the todo card showed `明天买牛奶`; `打开天气查北京，再打开世界时钟看东京时间` returned weather plus world-clock update and the page showed Beijing/Tokyo. Remaining gaps from the same run: `关闭，啊，这个，音乐`, `暂停音乐，同时打开新闻`, and `开始工作` could not complete without Realtime fallback; `帮我放点轻松的音乐，然后把倒计时设为 10 分钟` mounted music but stopped at `没有可播放的音乐` before setting the countdown; `把音乐收了` removed the music widget. | partial | This is the first local authenticated Milestone N run. It proves preview/confirm and several multi-tool local CommandPlan paths on the real page, but does not mark Milestone N complete because several complex/noisy commands still depend on Realtime-2 model fallback and the local `OPENAI_API_KEY` is empty. |
| 2026-06-17 21:50 CST | Execution plan v2 Realtime-2 fallback error surfacing | `开始工作`, `暂停音乐，同时打开新闻` | Fixed `OpenAIRealtimeWebRtcAdapter` text fallback error handling so non-2xx `/api/realtime/tool-call` responses no longer return `null` and masquerade as `我没听懂`. Adapter now parses the JSON `error` code and throws it to the harness/UI. On the real local page, `开始工作` and `暂停音乐，同时打开新闻` both posted to `http://localhost:5173/api/realtime/tool-call`, received HTTP 503 because the local server key is empty, and the assistant dock displayed `OPENAI_API_KEY_MISSING` / `失败：OPENAI_API_KEY_MISSING`. Added adapter regressions for `requestCommandPlan` and `requestToolCall` endpoint errors. | pass | This preserves the intended ownership split: complex commands fall through to Realtime-2 text plan fallback instead of local regex enumeration, and all model/fallback output still returns through harness validation before execution. Live model parsing remains blocked until a valid local `OPENAI_API_KEY` is available. |
| 2026-06-17 21:57 CST | Execution plan v2 Milestone D/N failure propagation fix | `帮我放点轻松的音乐，然后把倒计时设为 10 分钟` | Updated shortcut CommandPlan execution so a normal failed group does not stop later independent groups; `needs_confirmation` still queues the remaining groups and `cancelled` still stops execution. Added a Harness regression where `music.play` returns `没有可播放的音乐` but the following countdown group still executes. Real local page verification on `工作台测试` returned `没有可播放的音乐；已添加小工具，已设置并启动倒计时`, and DOM evidence showed the countdown widget at `00:09:59`. | pass | This closes the Milestone N gap where an unavailable music result prevented an independent countdown command from running. Related dependency chains still rely on explicit `dependsOn` or target resolution failures; this change does not bypass confirmation gates. |
| 2026-06-17 22:05 CST | Execution plan v2 Realtime session failure diagnostics | real-domain `连接 Realtime` failure reported as `Realtime 会话创建失败` | Added safe nested-error extraction for `/api/realtime/session` failures so OpenAI upstream status, code/type, param, and message are surfaced in the assistant dock instead of being collapsed to the generic `Realtime 会话创建失败`. Added API, adapter, and Dock regressions for an upstream `OPENAI_REALTIME_SESSION_CREATE_FAILED` response such as `status 400 / unknown_parameter / session.output_modalities`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/components/VoiceAssistantDock.test.ts api/realtime/session.test.ts`. | pass | This does not mark live Realtime as connected yet; it makes the real-domain blocker diagnosable on the next connection attempt without leaking API keys or private payloads. |
| 2026-06-17 22:08 CST | Execution plan v2 Realtime connection status classification | session endpoint failures vs WebRTC/SDP failures | Added `resolveRealtimeConnectFailureStatus` so `/api/realtime/session` failures such as `OPENAI_API_KEY_MISSING`, `AUTH_INVALID`, `REALTIME_CLIENT_SECRET_MISSING`, and nested `OPENAI_REALTIME_SESSION_CREATE_FAILED(...)` update runtime status as `session_failed`, while SDP/WebRTC channel failures remain `failed`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/components/VoiceAssistantDock.test.ts`. | pass | This advances Milestone N/Realtime connection stability evidence by separating session creation blockers from later voice-channel failures; live connection still needs a real-domain retry after the latest commit is served. |
| 2026-06-17 22:17 CST | Execution plan v2 Milestone N evidence outlet and Realtime config wait | `Realtime 会话配置未生效，请重试。`, page command diagnostics | Changed WebRTC `connect()` so it does not resolve until `session.updated` confirms Realtime configuration. Realtime `error` events during `session.update` now reject with a visible `REALTIME_SESSION_UPDATE_FAILED(...)` detail instead of timing out silently; update timeout remains `REALTIME_SESSION_UPDATE_TIMEOUT`. Added a redacted Harness diagnostics snapshot and published it to `window.__xiaozhuobanAssistantDiagnostics` after text commands for real-page acceptance capture: raw input, normalized text, segments, candidate modules, CommandPlan summary, execution groups, tool statuses, route, Realtime usage, confirmation/learning flags, and only argument keys (not argument values). Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/AssistantHarness.test.ts src/assistant/openaiRealtimeAdapter.test.ts src/components/VoiceAssistantDock.test.ts`. | pass | This directly supports Milestone N recording requirements without sending full private context or exposing clipboard/todo/note argument values. Live Realtime still requires a real-domain retry to determine whether the next failure is a concrete `session.update` schema error or a successful `session.updated`. |
| 2026-06-17 22:25 CST | Execution plan v2 Realtime backend/session-update diagnosis | `Realtime 会话配置未生效，请重试。` | Checked the local backend path and Realtime session code after the real page reported a configuration timeout. The server-side `/api/realtime/session` path already surfaces OpenAI client-secret creation errors; this message now points to the post-SDP WebRTC configuration stage, where `connect()` waits for `session.updated`. Fixed an initialization race: if the user connects voice before Harness has injected current tools, the adapter now sends the built-in selector-only tool catalog as an initial `session.update` instead of waiting on an empty tool list until `REALTIME_SESSION_UPDATE_TIMEOUT`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts`. | pass | The fix preserves Realtime-2 two-stage parsing and does not send full desktop context: the fallback update exposes only `assistant.select_tool` and the initial tool catalog. Live real-domain retry is still needed to prove the upstream Realtime session returns `session.updated`. |
| 2026-06-18 08:42 CST | 200 voice scenario Harness matrix | 200 distinct simulated voice transcripts covering app shell, board/window controls, note/todo/clipboard/countdown/weather/calculator/converter/translate/world-clock/headline/market/music/tv/recorder/dial-clock/message-board, simple commands, ambiguous commands, multi-step commands, parallel commands, and confirmation flows | Added executable `voiceScenarioMatrix.test.ts`, which sends 200 deterministic voice-transcript scenarios through `AssistantHarness.handleUserInput` with command trace ids, a simulated Realtime-2 CommandPlan fallback, real action execution, widget capability stubs, confirmation continuation, and diagnostics assertions. First run found 32 failures; fixes included routing `聚焦/切到` widget-window commands to `widget.focus`, routing `置顶/放最前/别被挡住` to `widget.bring_to_front`, suppressing music playback for explicit `不一定播放/不要播放` wording, and preventing weather/countdown state rules from stealing window-open commands such as `聚焦天气卡片` and `再打开一个倒计时`. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/voiceScenarioMatrix.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, and `pnpm --filter @xiaozhuoban/web typecheck`. | pass | This is a broad deterministic voice-chain simulation, not a live microphone/WebRTC run. It proves Harness routing, Realtime-plan fallback execution, diagnostics trace ids, confirmation continuation, and tool execution across 200 scenarios. Live browser Realtime validation remains a separate requirement before the full active goal can be marked complete. |
| 2026-06-18 08:48 CST | 200 voice scenario full registered-tool coverage audit | Actual `ActionRegistry` tool names from the scenario Harness | Extended `voiceScenarioMatrix.test.ts` so the matrix now reads every registered app, board, widget-state, and mounted widget-capability tool from `ActionRegistry` and fails if any tool name is absent from the 200 simulated voice scenarios. Added an explicit `board.add_widget` voice scenario because existing open-widget cases intentionally focus already-mounted widgets. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/voiceScenarioMatrix.test.ts`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/voiceScenarioMatrix.test.ts src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm --filter @xiaozhuoban/web build`. | pass | The matrix now proves both breadth by scenario count and breadth by registered tool catalog. Live microphone/WebRTC Realtime validation remains separate. |
| 2026-06-18 09:04 CST | Production Realtime music-plan repair and live page retest | `播放王菲的红豆`, `关闭留言板`, `播放轻松音乐`, `打开时钟`, `整理桌面` on `https://xiaozhuoban.bqxb.org/app` | Production text-command validation first exposed two Realtime plan-shape failures: model plans used `keyword` instead of `query`, `type` instead of `definitionId`, and sometimes omitted `board.add_widget` when no target music widget existed. Added adapter-side normalization that maps known aliases into strict schema fields, inserts a safe `board.add_widget` before widget-detail commands when the target widget is missing but a definition exists, and preserves Harness validation. Added Harness support for resolving `planned_widget_<type>` after add-widget success. Verified with adapter/Harness regressions, the 200 voice scenario matrix, typecheck, core tests, production build, and deployed `dpl_4Pvbw3febGWJjBeBRHNg6U7V5qKd` serving `App-D2cYDKv9.js`. Live production retest showed `播放王菲的红豆` added the music player and started Apple Music playback with Faye Wong results; `关闭留言板` removed the message board without sending text; `播放轻松音乐` changed the music search box to `轻松` and started a new result; `打开时钟` added a clock widget; `整理桌面` opened the expected confirmation. | pass | This is production text-command validation through the visible assistant input and Realtime text planning endpoint, not microphone/WebRTC audio validation. Local `.env.local` still contains empty quoted OpenAI/Music values, so local Realtime remains blocked unless env is refreshed. |
| 2026-06-18 09:10 CST | Production Realtime WebRTC connection retry | `连接语音` on `https://xiaozhuoban.bqxb.org/app` | Production connection previously stayed in `连接中` after `/api/realtime/session` returned 200, so added client-side connect timeouts around session and SDP requests to prevent indefinite hanging and surface `REALTIME_CONNECT_TIMEOUT(...)` if a browser/upstream call stalls. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/assistant/AssistantHarness.test.ts src/assistant/voiceScenarioMatrix.test.ts`, `pnpm --filter @xiaozhuoban/assistant-core test`, `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm --filter @xiaozhuoban/web build`; deployed `dpl_5o83c88cQP6Mcu9zQaSvuPpreC5F` serving `App-Dqp9sFWu.js`. Live production retest showed the assistant changing to `语音已连接，可以直接说话。` with the button changing to `断开语音`; browser network showed `/api/realtime/session => 200` and `https://api.openai.com/v1/realtime/calls?model=gpt-realtime-2 => 201`. | pass | This proves production session creation, SDP exchange, data-channel/session configuration, and visible connected state. It does not prove spoken audio command execution because this automated browser run did not inject a real microphone utterance. |
| 2026-06-18 09:18 CST | 700 voice scenario command catalog | 200 existing matrix commands plus 500 broader complex scenario commands | Added `scripts/generate-voice-scenario-command-catalog.mjs` and generated `docs/realtime-voice-scenario-command-catalog-700.md`. The catalog expands the current 200 `voiceScenarioMatrix.test.ts` commands, then adds 500 broader scenarios covering app shell, board management, widget lifecycle, music login/playback, TV, weather, time, note/todo, clipboard, translation, calculator/converter, news/market, recorder, message board safety, cross-tool workflows, ambiguity/corrections, confirmation, accessibility, productivity, travel, module loading, account state, diagnostics, and Realtime concurrency. Verified with the generator's built-in assertions and a separate Markdown audit: 700 numbered commands, 200 existing, 500 new, contiguous 001-700 numbering, and zero duplicate command texts. | pass | This is a command-catalog and static validation deliverable for future scenario expansion, not an executable Harness or live microphone run by itself. |
| 2026-06-18 11:36 CST | 700 voice scenario catalog simulation gate | `docs/realtime-voice-scenario-command-catalog-700.md` | Added `scripts/simulate-voice-scenario-catalog.mjs`, which parses every numbered command, assigns expected tool/module coverage, flags high-risk misroutes such as closing the message board being interpreted as sending text, and writes `docs/realtime-voice-scenario-catalog-simulation-report.md` with one result row per command. First run found 45 review items, including missing sidebar/fullscreen aliases, qualitative music commands classified as unknown, and false-positive message-board send hazards for commands that only mention closing. Tightened the simulation rules to distinguish execution from quoting/logging/translation, added virtual diagnostics/auth-state expectations for monitoring/account-status scenarios, and added `voiceScenarioCatalogSimulation.test.ts` as a Vitest gate. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/voiceScenarioCatalogSimulation.test.ts src/assistant/voiceScenarioMatrix.test.ts`: 203 tests passed, including 700/700 classified catalog commands and the existing 200 executable Harness scenarios. | pass | This is the first 700-item simulation layer and an auditable per-command report. It does not yet prove that all 700 commands execute through `AssistantHarness`; the next layer should drive more catalog commands through Harness with deterministic Realtime plans and browser/page sampling. |
| 2026-06-18 11:39 CST | 700 voice scenario Harness simulation gate | `docs/realtime-voice-scenario-catalog-simulation-report.md` | Added `voiceScenarioCatalogHarness.test.ts`, which parses the 700 per-command simulation report, creates a deterministic Realtime `CommandPlan` for each command, forces the command through `AssistantHarness.handleUserInput`, validates the plan against a simulated `ActionRegistry`, sends simulated tool results back through the Realtime adapter, and writes `docs/realtime-voice-scenario-catalog-harness-report.md` with one pass/fail row per command. Fixed the test to use a real empty `IntentShortcutRouter` instead of a structural mock so TypeScript sees the private router field correctly. Verified with `pnpm --filter @xiaozhuoban/web test -- src/assistant/voiceScenarioCatalogSimulation.test.ts src/assistant/voiceScenarioCatalogHarness.test.ts src/assistant/voiceScenarioMatrix.test.ts` and `pnpm --filter @xiaozhuoban/web typecheck`: 204 tests passed and typecheck passed. | pass | This proves the 700-command catalog can traverse the Harness + Realtime-plan + local validation + tool-result feedback path without microphone authorization. The registry is simulated, so real widget side effects remain covered only by the existing 200 executable Harness matrix and should be expanded in later slices. |
| 2026-06-18 11:40 CST | Assistant regression bundle after 700 simulation gates | Realtime adapter, text tool-call planning, Harness, acceptance, 200 matrix, 700 catalog gates | Ran the broader assistant regression bundle after adding the 700 catalog gates: `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts src/assistant/openaiRealtimeAdapter.test.ts src/assistant/realtimeTextToolCall.test.ts src/assistant/voiceScenarioCatalogSimulation.test.ts src/assistant/voiceScenarioCatalogHarness.test.ts src/assistant/voiceScenarioMatrix.test.ts`, which passed 318 tests. Also ran `pnpm --filter @xiaozhuoban/web typecheck` and `pnpm --filter @xiaozhuoban/web build`; both passed. Attempted local page sampling with Playwright, but `/app` redirected to `/login` and no existing local test account or dev-auth bypass was present, so page-level sampling was not completed in this slice. | partial | Browser/microphone-independent simulation is now covered by tests and reports. Real page sampling still needs an authenticated session, a safe test account, or an explicit local dev-auth bypass before it can verify visible UI behavior. Chrome extension automation also remained unavailable in this environment. |
| 2026-06-18 11:54 CST | Plan B text-only Realtime connection | No-microphone Realtime text command path | Added `connectTextOnly()` to the WebRTC adapter so it skips microphone permission and `getUserMedia`, still creates a Realtime data channel, waits for `session.updated`, and sends text commands as `conversation.item.create` with `input_text` plus `response.create` using `output_modalities: ["text"]`. Exposed `connectTextOnly` and `sendRealtimeTextCommand` through the assistant runtime. Added a 700-command text-only Realtime gate that verifies every catalog command is serialized into the official text event shape. Verified with `pnpm --filter @xiaozhuoban/web test -- src/components/VoiceAssistantDock.test.ts src/assistant/openaiRealtimeAdapter.test.ts src/assistant/createRealtimeAssistantRuntime.test.ts src/assistant/voiceScenarioCatalogTextOnlyRealtime.test.ts`: 61 tests passed. | pass | This follows the official Realtime conversation docs for text input and text output. It avoids microphone authorization entirely, but still depends on the backend `/api/realtime/session` endpoint and a valid authenticated production/dev session for a live connection. |
| 2026-06-18 11:55 CST | Plan B UI wiring | Assistant Dock text-only entry and text routing | Added a visible `文字 Realtime` Dock button alongside `连接语音`. When Realtime is connected and no local confirmation is pending, the assistant input sends text to `sendRealtimeTextCommand`; while disconnected or during confirmation it keeps the existing local Harness path so `确认/取消` remains reliable. Added Dock regression coverage for the routing predicate and text-channel error messages. | pass | Local Vite page showed both `连接语音` and `文字 Realtime`. The live text-only backend connection was not clicked in this local E2E run because the E2E auth bypass has no real Supabase access token; protocol coverage is supplied by adapter/runtime tests. |
| 2026-06-18 11:56 CST | Local real-page sample after E2E auth bypass | `关闭留言板`, `打开表盘时钟`, `整理桌面` on `http://localhost:5174/app` | Added `VITE_XIAOZHUOBAN_E2E_AUTH_BYPASS=true` support so local Playwright can enter `/app` with an in-memory repository instead of being redirected to `/login`. Real-page Playwright verification: initial snapshot showed `留言板`; after `关闭留言板`, the message-board widget disappeared and Dock history showed `已删除小工具`; after `打开表盘时钟`, the page showed a BALMUDA dial-clock widget; after `整理桌面`, the Dock showed `确认执行 board.auto_align 吗？`, clicking `确认` changed the visible operation to `完成：已整理桌面小工具`. | pass | This is frontend DOM evidence for actual page side effects, not only Harness feedback. It is a small first group sample; the remaining catalog groups still need additional browser sampling and/or production Realtime runs. |
| 2026-06-18 11:57 CST | Plan B final regression and UI smoke | Assistant regression suite, typecheck, build, local page quick command | Ran `pnpm --filter @xiaozhuoban/web test -- src/assistant/assistantAcceptance.test.ts src/assistant/AssistantHarness.test.ts src/assistant/openaiRealtimeAdapter.test.ts src/assistant/createRealtimeAssistantRuntime.test.ts src/assistant/realtimeTextToolCall.test.ts src/assistant/voiceScenarioCatalogSimulation.test.ts src/assistant/voiceScenarioCatalogHarness.test.ts src/assistant/voiceScenarioCatalogTextOnlyRealtime.test.ts src/assistant/voiceScenarioMatrix.test.ts src/components/VoiceAssistantDock.test.ts`: 344 tests passed. Ran `pnpm --filter @xiaozhuoban/web typecheck` and `pnpm --filter @xiaozhuoban/web build`; both passed with the existing large chunk warning. After the UI wiring, Playwright snapshot confirmed both `连接语音` and `文字 Realtime` buttons are visible, then `关闭留言板` again removed the message-board widget and showed `完成：已删除小工具`. | pass | Chrome extension automation remains unavailable in this environment (`Browser is not available: extension`), so page verification used the Playwright CLI on the local Vite app. |
| 2026-06-18 12:08 CST | 700 catalog real-page group 001-011 | App shell/window controls plus low-confidence board actions | Attempted Chrome extension automation first: Chrome was running, the Codex Chrome Extension was installed/enabled in the selected Default profile, and the native host manifest was correct, but the extension channel still returned `Browser is not available: extension`. Continued with Playwright CLI on `http://localhost:5174/app` using `VITE_XIAOZHUOBAN_E2E_AUTH_BYPASS=true`. First run found a real page bug: `把左边栏先藏起来` fell through to model/Reatime and displayed `AUTH_REQUIRED`. Fixed app shell shortcuts so sidebar, app fullscreen, settings, command palette, and AI dialog commands route as high-confidence local commands without stealing widget fullscreen. Added `scripts/playwright-real-page-app-shell-group.js`, which drives commands 001-011 through the visible assistant input, mocks only the low-confidence Realtime plan responses for board create/rename/switch, and verifies DOM side effects. Final run passed 11/11: sidebar hide/show changed the toolbar/sidebar; fullscreen enter/exit reported completion; settings/search/AI surfaces opened; desktop auto-align required confirmation and completed; board create/rename/switch produced visible `学习桌板`, `夜间工作`, and active `工作台桌板`. | pass | This is real frontend execution evidence for catalog 001-011. Commands 009-011 intentionally keep the low-confidence Realtime path and use a network mock for `/api/realtime/tool-call` because local E2E auth bypass has no real backend token. A learning-candidate prompt after model execution is cancelled at script teardown to keep later groups clean. |
| 2026-06-18 12:19 CST | 700 catalog real-page group 012-019 | Widget window controls and lifecycle actions | Added `scripts/playwright-real-page-widget-window-group.js`, which enters `/app` with local E2E auth bypass, seeds TV/music/weather through the visible assistant input, then drives catalog commands 012-019 through the same assistant input. For low-confidence widget window actions, the script mocks only the `/api/realtime/tool-call` plan responses while still executing through Harness and ActionRegistry. Final run passed 8/8: `把电视拖到右上角` moved the TV rect from x=320/y=92.8 to x=1200/y=52.8; `把电视面板调大一点` changed TV width from 240 to 498; `把音乐播放器放最前` raised music zIndex from 3 to 5; `聚焦天气卡片` gave weather `is-focused`; `全屏看电视` produced a fullscreen widget id matching the TV widget; `关闭留言板` removed the message-board widget from `[data-widget-id]`; `打开一个表盘时钟` added a BALMUDA dial-clock widget; `新建便签实例用于测试` added a note widget. | pass | This is real frontend DOM evidence for widget movement, resize, z-index, focus, fullscreen, removal, and creation. The group did not require microphone authorization. Chrome extension automation remained unavailable, so verification used Playwright CLI. |
| 2026-06-18 12:20 CST | 700 catalog real-page group 020-027 | Weather questions and city switching | Added `scripts/playwright-real-page-weather-group.js`, which enters `/app` with local E2E auth bypass, drives catalog commands 020-027 through the visible assistant input, and verifies the actual weather widget count and city label after each command. First real-page run found `查北京今天冷不冷` fell through to Realtime and displayed `AUTH_REQUIRED` because the utterance did not literally include `天气`. Fixed local weather routing so city-specific natural weather questions such as `冷不冷`, `热不热`, `气温`, `下雨`, `风大`, `出门`, and `穿什么` route as high-confidence weather commands. Final run passed 8/8: Beijing, Shanghai, Los Angeles, Hangzhou, Wuhan, Boston, Guangzhou, and Chengdu all updated the visible weather widget city text with no Realtime/Auth error banner. | pass | This is real frontend DOM evidence for actual weather widget creation and city switching. It also preserves the rule that ambiguous/low-confidence commands can still go to Realtime; this fix only captures city-specific natural weather questions. Chrome extension automation remained unavailable, so verification used Playwright CLI. |
| 2026-06-18 12:27 CST | 700 catalog real-page group 028-039 | Countdown, reminder, note, and todo commands | Added `scripts/playwright-real-page-countdown-note-todo-group.js`, which drives commands 028-039 through the visible assistant input and verifies actual DOM state: countdown display seconds, pause/resume timing, note textarea value, todo text, and destructive note-clear confirmation. First real-page exploration found `暂停现在的计时器`, `继续刚才那个倒计时`, `重置倒计时`, explicit note writes without an existing note widget, `清空便签内容`, and explicit todo add without an existing todo widget could fall to Realtime/Auth because their confidence was below the 0.9 local threshold. It also found `设置二十五秒计时` parsed as 15 seconds. Fixed high-confidence routing for explicit countdown control, note write/clear, and todo/reminder add; added default text for due-time-only reminders such as `十分钟后提醒我`; and fixed Chinese compound numbers so `二十五秒` becomes 25 seconds. Final run passed 12/12: 3-minute countdown added, reminder todo added with a due time, pause held the display stable, resume decremented it, reset restored 3 minutes, 25 seconds/30 minutes/1 hour displayed correctly, note write/append updated the textarea, confirm cleared the note, and `买咖啡豆` appeared in the todo widget. | pass | This is real frontend DOM evidence, not just Harness feedback. The note clear path intentionally requires clicking the visible confirmation button before the textarea is expected to clear. Chrome extension automation remained unavailable; Chrome was running and the Codex extension was installed/enabled, but extension communication still returned `Browser is not available: extension`, so verification used Playwright CLI. |
| 2026-06-18 12:44 CST | 700 catalog real-page group 040-051 | Daily tools: reminders, todo completion, clipboard, translation, calculator, converter, world clock | Added `scripts/playwright-real-page-daily-tools-group.js`, which drives commands 040-051 through the visible assistant input and verifies real DOM state for todo due times, todo completion, clipboard pin/clear behavior, translate source/language, calculator display, converter results, and world-clock cities. First exploration found several explicit commands fell to Realtime/Auth because their local confidence was below the 0.9 threshold or because Chinese number phrases were not parsed. Fixed explicit high-confidence local routing for todo completion, clipboard add/pin/clear, translate draft, calculator Chinese arithmetic, and converter Chinese-number units while preserving ambiguous qualitative commands for Realtime. Final run passed 12/12: `提交报告` due 2026/6/19 09:00 appeared, `买牛奶` was completed/removed, `演示账号` and pinned `项目口令 demo` behaved correctly, `hello world` and `你好` populated translation, `十二加三十` showed 42, `2斤/十二米/两公斤` converted to 1000 g / 0.012 km / 2000 g, and Beijing/London/New York appeared in the world clock. | pass | This pass is intentionally scoped to high-confidence local commands with real frontend execution evidence. It is not counted as Realtime-2 parsing coverage. |
| 2026-06-18 12:44 CST | Realtime-2 text-only live gate | `连接文字 Realtime`, `关闭留言板`, `打开一个表盘时钟` | Added `XIAOZHUOBAN_E2E_REALTIME_AUTH_BYPASS=true`, a non-production-only local API auth bypass for Realtime E2E, and `scripts/playwright-realtime2-text-live-group.js`, which clicks the real `文字 Realtime` button, skips microphone permission, and expects Realtime diagnostic events plus DOM side effects before marking commands pass. The live run reached `/api/realtime/session` in text mode and recorded diagnostics `realtime.connect.start`, `realtime.microphone.permission=skipped`, `realtime.session.request=missing_auth`, then failed with `OPENAI_API_KEY_MISSING`; both `apps/web/.env.local` and `.vercel/.env.production.local` currently contain quoted empty OpenAI keys. | blocked | This is the corrected Realtime-2 gate requested by the user: it does not use local regex fallback or mocked `/api/realtime/tool-call` responses. Realtime-2 parsing/execution is blocked until a valid server-side `OPENAI_API_KEY` is available locally or the production endpoint can be tested with an authenticated session. |
| 2026-06-18 13:19 CST | Production Realtime-2 text-only music parse gate | `播放陈奕迅的十年` on `https://xiaozhuoban.bqxb.org/app` | Corrected the test strategy after user feedback: local high-confidence tool groups remain frontend execution smoke tests only and are not counted as Realtime-2 parsing coverage. Added `scripts/playwright-realtime2-music-parse-live.js`, created a temporary production E2E account through the public register flow, connected `文字 Realtime`, and verified the real Realtime data-channel event before Harness execution. The passing run captured `realtime.function_call.tool` with `toolName: music.play` and safe diagnostic `data.query: "陈奕迅 十年"`, followed by `realtime.tool_result.send` status `success`, visible operation `完成：已开始播放音乐`, music search input `陈奕迅 十年`, and the music panel showing `Apple Music 播放中 / 十年 / Eason Chan`. | pass | This is real production Realtime-2 parsing plus Harness execution plus frontend UI evidence; it does not rely on local shortcut parsing or mocked `/api/realtime/tool-call`. Fixes in this slice also prevent natural music/news/market/message-board commands from being promoted above the local 0.9 shortcut threshold, prevent `播放王菲的红豆` from falling through to focused TV playback, and make text-only WebRTC create a `recvonly` audio transceiver so OpenAI accepts the SDP without microphone access. |
| 2026-06-18 13:31 CST | 700 catalog real-page group 064-085 | Music/TV/recorder controls and widget close commands | Added `scripts/playwright-real-page-media-controls-close-group.js`, which drives commands 064-085 through the visible assistant input on `http://localhost:5174/app`, seeds a deterministic iTunes result set for music queue verification, injects a fake `MediaRecorder` so recorder UI can be tested without microphone permission, and verifies visible widget state after each command. First run found real execution gaps: `电视全屏` did not route to the TV fullscreen capability, and `上一首` without saying `歌` did not target the existing music widget. Fixed both in the shortcut router. Final run passed 22/22: music resumed, moved next to `红豆`, moved previous back to `十年`; TV selected CCTV-13, played/pause/fullscreen completed; recorder start/stop/play/pause changed the visible recorder state; and music, TV, recorder, weather, countdown, todo, clipboard, translate, calculator, market, and headline widgets were removed from `[data-widget-id]` nodes. | pass | This is real frontend execution evidence, not Realtime-2 parsing coverage. Recorder stop/play/pause remain below the local 0.9 threshold and were executed through a mocked `/api/realtime/tool-call` plan to preserve the product rule that low-confidence commands go through Realtime rather than being forced into local shortcuts. |
| 2026-06-18 13:37 CST | 700 catalog real-page group 086-106 | World-clock close, widget focus, and existing-widget open/focus commands | Added `scripts/playwright-real-page-focus-add-group.js`, which enters `http://localhost:5174/app` with local E2E auth bypass, seeds the required widgets through the visible assistant input, and drives commands 086-106 through the same input. The script verifies that `把世界时钟收起来` removes the world-clock widget from `[data-widget-id]`, then checks that focus commands for music, TV, recorder, weather, todo, message board, dial clock, and note apply `is-focused`, and that `再打开一个...` commands for existing singleton widgets focus the existing widget rather than silently doing nothing. Final run passed 21/21 with visible operation evidence such as `完成：已删除小工具`, `完成：已聚焦小工具`, and focused widget classes. | pass | This is real frontend execution evidence, not Realtime-2 parsing coverage. It preserves the current matrix semantics that `再打开一个` for mounted singleton widgets focuses the existing widget; explicit multi-instance creation remains covered by commands such as `新建便签实例用于测试`. |
| 2026-06-18 13:52 CST | 700 catalog execution-lane regrouping | `docs/realtime-voice-scenario-execution-groups.md` | Centralized the 700-command simulation grouping so each command now has an execution lane: `shortcut-local` only for high-confidence local shortcuts, and `realtime-2-required` for low-confidence, semantic, constrained, multi-tool, repeated scenario, music/news/market, and post-106 commands. The generated grouping now classifies 82 commands as shortcut-local and 618 commands as realtime-2-required, with semantic music commands 060-062 and multi-intent command 107 explicitly in the Realtime-2 lane. Added a production live-gate script draft, `scripts/playwright-realtime2-low-confidence-live.js`, to test low-confidence Realtime-2 parsing for music search/play, music+weather multi-tool, and widget removal without microphone access. Verified with `node scripts/simulate-voice-scenario-catalog.mjs` and `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/assistant/voiceScenarioCatalogSimulation.test.ts src/assistant/voiceScenarioCatalogHarness.test.ts`: 42 tests passed. | pass | This is a routing/test-campaign contract update, not a claim that all 618 Realtime-2 commands have passed live parsing. Mocked `/api/realtime/tool-call` runs remain Harness/frontend smoke only; Realtime-2 parsing success requires live `realtime.function_call.tool` diagnostics plus Harness result plus visible frontend evidence. |
| 2026-06-18 15:31 CST | Production Realtime-2 low-confidence live gate | `我想听点放松的不一定播放`, `来个周杰伦经典`, `播放陈奕迅十年，然后查上海天气`, `关闭音乐和留言板` | Deployed production twice and ran `scripts/playwright-realtime2-low-confidence-live.js` against `https://xiaozhuoban.bqxb.org/app` using a temporary Supabase account and text-only Realtime. Confirmed real Realtime-2 plan/execute plus Harness/frontend success for `来个周杰伦经典` (`music.play`, music query `周杰伦经典`, Jay Chou result playing), `播放陈奕迅十年，然后查上海天气` (`music.play` + `weather.set_city`, music query includes `陈奕迅/十年`, weather shows Shanghai), and `关闭音乐和留言板` (two `widget.remove` operations and both widgets removed from DOM). The first run also confirmed `我想听点放松的不一定播放` as `music.search` with music query `放松`; the next run exposed a model-argument drift where Realtime-2 sent extra `boardId/notAutoPlay` to `music.search`. Fixed the live-gate assertion to count `assistant.operation` success as Harness execution evidence, and fixed Realtime argument normalization so music tools fold `artist/song` aliases into `query` and drop music-irrelevant `boardId/notAutoPlay/autoPlay/autoplay` before strict Harness validation. Verified locally with `node --check scripts/playwright-realtime2-low-confidence-live.js`, `pnpm --filter @xiaozhuoban/web test -- src/assistant/openaiRealtimeAdapter.test.ts src/assistant/AssistantHarness.test.ts src/components/VoiceAssistantDock.test.ts src/assistant/realtimeTextToolCall.test.ts` (110 tests), `pnpm --filter @xiaozhuoban/web typecheck`, and `pnpm --filter @xiaozhuoban/web build`. | partial | Current production evidence has 3/4 passing after the second deploy, with successful Realtime-2 commands for music play, multi-tool music+weather, and multi-widget removal. The final production redeploy needed to validate the `boardId/notAutoPlay` fix was blocked by Vercel quota: `api-deployments-free-per-day` / more than 100 deployments, retry after 24 hours. |
| 2026-06-18 16:22 CST | 700 catalog real-page group 107-131 | Multi-tool scenario 1, semantic media/info commands, widget state commands | Added `scripts/playwright-real-page-scenario1-group.js`, which drives commands 107-131 through the visible assistant input on `http://localhost:5174/app` with local E2E auth bypass, clean localStorage/sessionStorage/IndexedDB state, fake `MediaRecorder`, deterministic iTunes responses, and mocked `/api/realtime/tool-call` only for low-confidence Realtime plan responses. The script records `realtimeHits` per command so mocked Realtime-plan execution is not confused with local shortcut execution or live Realtime-2 parsing. First real-page run exposed a real frontend bug: `电影频道打开，场景1` produced `tv.play` success with `channelName=CCTV6` and `channelUrl=...cctv6.m3u8`, but the visible TV media source stayed on the first subscription channel because the async playlist load overwrote externally selected channel URLs. Fixed TV selection preservation by keeping external tool-selected URLs in the loaded channel list, added CCTV6 fallback channel matching, and kept generic TV aliases below the local shortcut threshold. Final Playwright run passed 25/25: music+weather, shell settings/sidebar, TV fullscreen, note+todo, widget removal, weather/music/news/recorder/clipboard/world-clock/market/calculator/translate/message-board/dial-clock commands all showed visible DOM state; command 122 now shows `<video src="http://www.douzhicloud.site:35455/gaoma/cctv6.m3u8">` and visible `CCTV6`. | pass | This is real frontend/Harness execution evidence for catalog 107-131. It is not counted as live Realtime-2 parsing coverage because the low-confidence model plans were mocked locally; live Realtime-2 coverage remains tracked separately by the production text-only gate. Chrome extension automation remained unavailable, so verification used Playwright CLI with system Chrome. |
| 2026-06-18 16:32 CST | 700 catalog real-page group 132-191 | Repeated scenario groups 2-4 for semantic media/info/widget commands | Added `scripts/playwright-real-page-scenario-repeats-group.js`, which drives catalog commands 132-191 through the visible assistant input on `http://localhost:5174/app` with local E2E auth bypass, clean localStorage/sessionStorage/IndexedDB state, fake `MediaRecorder`, deterministic iTunes responses, and mocked `/api/realtime/tool-call` only for low-confidence Realtime plan responses. The first run passed scenario 2 but exposed a test isolation issue: after `音乐面板放大`, the music widget remained focused/enlarged and subsequent repeated low-confidence commands no longer exercised their intended Realtime/mock path. Updated the script to reload `/app` and re-seed the base widgets between repeated scenarios, matching the previous fullscreen-pollution isolation pattern. Final Playwright run passed 60/60 for scenes 2-4: repeated weather, music search/play, todo/reminder, translation, calculator, market, world clock, headline, recorder, TV CCTV6, message board, clipboard, dial-clock, command palette, AI dialog, board switch, TV bring-to-front, and music focus commands all showed visible DOM state. | pass | This is real frontend/Harness execution evidence for repeated catalog commands 132-191. It is not counted as live Realtime-2 parsing coverage because low-confidence model plans were mocked locally; the script records `realtimeHits` per command so this boundary remains visible. Chrome extension automation remained unavailable, so verification used Playwright CLI with system Chrome. |
| 2026-06-18 16:59 CST | 700 catalog real-page group 192-220 | Scene 5 repeats plus complex app-shell/settings/window commands | Added `scripts/playwright-real-page-app-shell-complex-group.js`, which can now run directly with Playwright and fails if any command assertion fails. The script drives commands 192-220 through the visible assistant input on `http://localhost:5174/app`, seeds base widgets, uses deterministic iTunes responses, and mocks `/api/realtime/tool-call` only for low-confidence or complex Realtime plan responses while recording `realtimeHits`. First real-page run failed 10/29 because complex commands such as `退出全屏，打开搜索面板，然后输入天气两个字` and `隐藏侧栏，打开 AI 小工具窗口，名字先叫每日摘要` were partially swallowed by local shortcuts. Fixed app-shell tool parameters so command palette accepts an initial query and AI dialog accepts an initial prompt, and made the Harness defer complex/multi-clause shortcut segments to Realtime instead of executing a partial local shortcut. Later runs exposed test issues around dialog cleanup, note widget id hydration, and `board.auto_align` confirmation; the final run passed 29/29 with visible DOM evidence: command palette prefilled `天气`/`世界时钟`, AI dialog prefilled `草稿`/`每日摘要`, settings/sidebar/fullscreen states changed, note focus applied, TV entered fullscreen on CCTV6, music/weather/todo/translate/calculator/market/headline/world-clock commands updated their target widgets, and `隐藏侧栏并把当前桌面整理整齐` reached the expected `board.auto_align` pending-confirmation state. | pass | This is real frontend/Harness execution evidence for catalog commands 192-220. It includes successful mocked Realtime-plan routing and frontend execution for low-confidence/complex commands, but it is not counted as live Realtime-2 parsing coverage. Live Realtime-2 success remains limited to the production text-only gates already logged separately. |
| 2026-06-18 17:31 CST | 700 catalog real-page group 221-240 | Board management, board-scoped widget setup, and confirmation-safe cleanup | Added `scripts/playwright-real-page-board-management-group.js`, which runs directly with Playwright, resets the local E2E app state per command, seeds target boards, drives commands 221-240 through the visible assistant input on `http://localhost:5174/app`, and records `realtimeHits` for mocked `/api/realtime/tool-call` plan responses. First runs exposed three real issues: board-management compound commands such as `新开旅行计划桌板，把天气、世界时钟和待办都放上去` were partially swallowed by local shortcuts; Realtime model plans using `board.add_widget` followed by `widget.move` kept `planned_widget_tv` instead of rewriting to the real widget id; and `widget.move` returned success even when the target widget did not exist. Fixed the complex shortcut deferral rule, moved planned-widget rewriting into the shared CommandExecutor path for model/Realtime plans, and made `widget.move` fail on missing targets. Final run passed 20/20: board create/switch/rename flows produced visible active boards (`晨间复盘`, `旅行计划`, `夜间工作桌板`, `音乐练习`, `语音回归测试`, `家庭事务`, `学习桌板`, `市场观察`, `今晚直播`, `临时桌板`, `会议记录`, `阅读桌板`), board-scoped widgets appeared as requested, dial clock entered night mode, recorder showed recording state, TV/weather move operations updated visible positions, and `整理桌板之后把留言板关闭，不要发送留言` removed the message board instead of sending a message. | pass | This is real frontend/Harness execution evidence for catalog commands 221-240. It uses mocked Realtime plans for low-confidence/complex commands and verifies DOM side effects; live Realtime-2 parsing coverage remains tracked only by the production text-only gates. |
